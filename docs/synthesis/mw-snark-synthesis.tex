\documentclass[runningheads]{llncs}

\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{listings}
\usepackage{url}

\lstset{
  basicstyle=\ttfamily\small,
  breaklines=true,
  columns=fullflexible,
  keepspaces=true,
  showstringspaces=false
}

\title{Recasting Classical Deductive Program Synthesis Examples in SNARK}
\titlerunning{Deductive Synthesis Examples in SNARK}

\author{Codex}
\authorrunning{Codex}
\institute{SNARK workspace notes, \url{https://github.com/nilqed/SNARK}}

\begin{document}

\maketitle

\begin{abstract}
SNARK supports answer extraction from refutation proofs, a mechanism that
connects automated theorem proving with deductive program synthesis.  This note
documents a small extension of the local SNARK example suite under
\texttt{examples/synthesis}.  The extension recasts classical
Manna--Waldinger developments as runnable SNARK examples: synthesis of the
unification algorithm, emergence of the binary-search paradigm in the
square-root approximation problem, subtractive greatest-common-divisor
computation, and quotient/remainder computation by recursive subtraction.  The
development also adds three further stress tests: stable merge, stable
partition, and a Boyer--Moore-style simplifier for normal-form IF-expressions
\cite{BoyerMoore1979}.  A separate MST development treats the classical
Kruskal, Jarnik--Prim, and Boruvka algorithms
\cite{Kruskal1956,Jarnik1930,Prim1957,Boruvka1926} as logical refinements of
a shared safe-extension skeleton, then extends that skeleton with red-rule
deletion, contraction, verification, replacement-edge, randomized filtering,
soft-heap, matroid, dynamic, and distributed refinements.  It is not a verbatim
reproduction of the original deductive-tableau proofs.  Instead, it isolates
the central constructive obligations and uses SNARK's conditional answers to
extract program terms from proofs.  It also includes executable Lisp
interpretations of the extracted terms, making explicit the gap between a proof
answer and a runnable program.

\keywords{SNARK \and Answer extraction \and Deductive synthesis \and
Unification \and Binary search \and Minimum spanning tree}
\end{abstract}

\section{Overview}

Deductive program synthesis treats program construction as theorem proving.
The desired program is specified by an existential theorem: for each admissible
input there exists an output satisfying the specification.  A constructive
proof of that theorem carries enough information to compute the output.  In
SNARK, this information is represented by answers attached to rows.  When a
proof closes, \texttt{(answer t)} can be read as the extracted witness term.

The local development adds a Manna--Waldinger synthesis example file.  It
contributes seven entry points:
\begin{lstlisting}
(mw-unification-variable-case)
(mw-unification-constant-case)
(mw-unification-compound-case)
(mw-square-root-binary-search-step)
(mw-square-root-binary-search-program)
(mw-gcd-subtractive-program)
(mw-quotient-remainder-program)
\end{lstlisting}
The aggregator \texttt{(mw-synthesis-examples)} runs all seven and returns their
extracted program terms.  The examples are also wired into
\texttt{snark-system.lisp} and \texttt{snark-examples.asd}, so rebuilding SNARK
compiles them with the rest of the example suite.

The same file also contains runnable interpretations:
\begin{lstlisting}
(mw-run-unify term1 term2)
(mw-run-sqrt r eps)
(mw-run-gcd a b)
(mw-run-quotient-remainder n d)
(mw-executable-synthesis-demos)
\end{lstlisting}
These are ordinary Common Lisp functions.  They are included to show where the
SNARK answer term stops and concrete execution begins.

A second file, \texttt{new-synthesis-examples.lisp}, adds examples that are not
claimed as historical Manna--Waldinger reconstructions but that exercise the
same answer-extraction interface:
\begin{lstlisting}
(new-stable-merge-program)
(new-stable-partition-program)
(new-if-normal-form-simplifier-program)
(new-synthesis-examples)
\end{lstlisting}
It also includes executable interpretations:
\begin{lstlisting}
(new-executable-synthesis-demos)
\end{lstlisting}

A third file, \texttt{new-mst-fol-synthesis.lisp}, adds the strict
non-probabilistic MST FOL layer:
\begin{lstlisting}
(new-mst-fol-synthesis-examples)
\end{lstlisting}
It replaces the ordinary MST named obligations with finite first-order
certificates for lists, graph edge membership, paths, cuts, components,
bridge-based acyclicity, finite weight comparison, exchange, blue/red rules,
Boruvka batch safety, and the Kruskal/Prim/Boruvka derivations.

A fourth file, \texttt{new-mst-prelude-synthesis.lisp}, introduces the broader
first-order MST prelude:
\begin{lstlisting}
(new-mst-prelude-synthesis-examples)
\end{lstlisting}
It reifies paths, cycles, cuts, finite edge sets, exchange trees,
contractions, samples, heap states, matroids, update streams, and message
schedules as first-order objects, then proves the core schematic lemmas used
by the algorithm layer, including the probabilistic and data-structure hooks.

A fifth file, \texttt{new-mst-synthesis.lisp}, develops minimum-spanning-tree
algorithms as a refinement hierarchy:
\begin{lstlisting}
(new-mst-synthesis-examples)
(new-run-mst graph :strategy :kruskal)
(new-run-mst graph :strategy :prim)
(new-run-mst graph :strategy :boruvka)
(new-run-mst-demo)
\end{lstlisting}

A sixth file, \texttt{new-mst-advanced-synthesis.lisp}, adds the standard
optimization and verification refinements around that hierarchy:
\begin{lstlisting}
(new-mst-advanced-synthesis-examples)
(new-run-mst-advanced-demo)
\end{lstlisting}

\section{Answer Extraction in the Development}

Each example follows the same pattern.  First, it initializes SNARK with
resolution and conditional answer creation:
\begin{lstlisting}
(initialize)
(use-resolution t)
(use-conditional-answer-creation t)
\end{lstlisting}
Next, it declares an abstract object language of constructors, predicates, and
program symbols.  The specification cases are asserted as implications.  The
goal is then proved with an answer template:
\begin{lstlisting}
(prove '(some-output-relation input ?result)
       :answer '(values ?result))
\end{lstlisting}
If two proof branches contribute different answers, SNARK combines them with
\texttt{ANSWER-IF}.  This is the key mechanism used here to expose conditional
branches in the synthesized program.

\section{Specification-to-Answer Catalogue}

This section records the complete middle layer for the examples: the informal
mathematical/logical specification, the actual SNARK input formulas, and the
answer extracted by SNARK.  Declarations of constructors and predicates are in
the source files; the listings here show the formulas sent to the prover and
the witness query.

\subsection{MW Unification: Variable Case}

Mathematically, the variable case is the occurs-check split:
\[
  \mathrm{occurs}(\mathrm{var}(v),t) \Rightarrow r=\mathrm{none},
  \qquad
  \neg\mathrm{occurs}(\mathrm{var}(v),t)
    \Rightarrow r=\mathrm{some}(\mathrm{bind}(v,t)).
\]
SNARK input:
\begin{lstlisting}
(assert '(implies (mw-occurs (mw-var v) term)
                  (mw-unify-out (mw-var v) term mw-none))
        :name 'mw-occurs-check-failure)
(assert '(implies (not (mw-occurs (mw-var v) term))
                  (mw-unify-out (mw-var v) term
                                (mw-some (mw-bind v term))))
        :name 'mw-variable-binding)
(prove '(mw-unify-out (mw-var v) term ?result)
       :answer '(values ?result))
\end{lstlisting}
Answer:
\begin{lstlisting}
(ANSWER-IF (MW-OCCURS (MW-VAR V) TERM)
  (VALUES MW-NONE)
  (VALUES (MW-SOME (MW-BIND V TERM))))
\end{lstlisting}

\subsection{MW Unification: Constant Case}

For constants, unification succeeds with the identity substitution exactly when
the symbols match:
\[
  \mathrm{same}(c,d) \Rightarrow r=\mathrm{some}(\mathrm{id}),
  \qquad
  \neg\mathrm{same}(c,d) \Rightarrow r=\mathrm{none}.
\]
SNARK input:
\begin{lstlisting}
(assert '(implies (mw-same-symbol c d)
                  (mw-unify-out (mw-const c) (mw-const d)
                                (mw-some mw-id)))
        :name 'mw-identical-constants)
(assert '(implies (not (mw-same-symbol c d))
                  (mw-unify-out (mw-const c) (mw-const d) mw-none))
        :name 'mw-distinct-constants)
(prove '(mw-unify-out (mw-const c) (mw-const d) ?result)
       :answer '(values ?result))
\end{lstlisting}
Answer:
\begin{lstlisting}
(ANSWER-IF (MW-SAME-SYMBOL C D)
  (VALUES (MW-SOME MW-ID))
  (VALUES MW-NONE))
\end{lstlisting}

\subsection{MW Unification: Compound Case}

For compound terms, first unify the heads.  If that fails, fail.  Otherwise
substitute the first unifier through the tails, unify the tails, and compose
the two substitutions if the second unification succeeds.
SNARK input:
\begin{lstlisting}
(assert '(implies
          (mw-fail (mw-unify m m1))
          (mw-unify-out (mw-app m n) (mw-app m1 n1) mw-none))
        :name 'mw-compound-first-fails)
(assert '(implies
          (and (not (mw-fail (mw-unify m m1)))
               (mw-fail
                (mw-unify
                 (mw-subst (mw-subst-of (mw-unify m m1)) n)
                 (mw-subst (mw-subst-of (mw-unify m m1)) n1))))
          (mw-unify-out (mw-app m n) (mw-app m1 n1) mw-none))
        :name 'mw-compound-second-fails)
(assert '(implies
          (and (not (mw-fail (mw-unify m m1)))
               (not
                (mw-fail
                 (mw-unify
                  (mw-subst (mw-subst-of (mw-unify m m1)) n)
                  (mw-subst (mw-subst-of (mw-unify m m1)) n1)))))
          (mw-unify-out
           (mw-app m n)
           (mw-app m1 n1)
           (mw-some
            (mw-compose
             (mw-subst-of (mw-unify m m1))
             (mw-subst-of
              (mw-unify
               (mw-subst (mw-subst-of (mw-unify m m1)) n)
               (mw-subst (mw-subst-of (mw-unify m m1)) n1)))))))
        :name 'mw-compound-compose)
(prove '(mw-unify-out (mw-app m n) (mw-app m1 n1) ?result)
       :answer '(values ?result))
\end{lstlisting}
Answer:
\begin{lstlisting}
(ANSWER-IF (MW-FAIL (MW-UNIFY M M1))
  (VALUES MW-NONE)
  (ANSWER-IF
    (MW-FAIL
      (MW-UNIFY
        (MW-SUBST (MW-SUBST-OF (MW-UNIFY M M1)) N)
        (MW-SUBST (MW-SUBST-OF (MW-UNIFY M M1)) N1)))
    (VALUES MW-NONE)
    (VALUES
      (MW-SOME
        (MW-COMPOSE
          (MW-SUBST-OF (MW-UNIFY M M1))
          (MW-SUBST-OF
            (MW-UNIFY
              (MW-SUBST (MW-SUBST-OF (MW-UNIFY M M1)) N)
              (MW-SUBST (MW-SUBST-OF (MW-UNIFY M M1)) N1))))))))
\end{lstlisting}

\subsection{MW Square Root: Binary-Search Step}

For a current lower endpoint \(z\), choose the right half exactly when
\((z+\epsilon)^2\leq r\):
\[
  (z+\epsilon)^2\leq r \Rightarrow a=z+\epsilon,
  \qquad
  \neg((z+\epsilon)^2\leq r) \Rightarrow a=z.
\]
SNARK input:
\begin{lstlisting}
(assert '(implies
          (mw-le (mw-square (mw-plus z eps)) r)
          (mw-sqrt-within r (mw-plus z eps) eps))
        :name 'mw-square-root-right-half)
(assert '(implies
          (not (mw-le (mw-square (mw-plus z eps)) r))
          (mw-sqrt-within r z eps))
        :name 'mw-square-root-left-half)
(prove '(mw-sqrt-within r ?answer eps)
       :answer '(values ?answer))
\end{lstlisting}
Answer:
\begin{lstlisting}
(ANSWER-IF (MW-LE (MW-SQUARE (MW-PLUS Z EPS)) R)
  (VALUES (MW-PLUS Z EPS))
  (VALUES Z))
\end{lstlisting}

\subsection{MW Square Root: Recursive Program}

The recursive skeleton returns zero at a large tolerance; otherwise it recurses
at doubled tolerance and applies the same right-half test:
\[
  \mathrm{large}(r,\epsilon) \Rightarrow a=0,
  \quad
  \neg\mathrm{large}(r,\epsilon) \Rightarrow
  a \in \{s,\;s+\epsilon\}
\]
where \(s=\mathrm{sqrt}(r,2\epsilon)\), choosing \(s+\epsilon\) exactly when
\((s+\epsilon)^2\leq r\).  SNARK input:
\begin{lstlisting}
(assert '(implies
          (mw-large-tolerance r eps)
          (mw-sqrt-within r zero eps))
        :name 'mw-square-root-base-case)
(assert '(implies
          (and (not (mw-large-tolerance r eps))
               (mw-le
                (mw-square
                 (mw-plus (mw-sqrt r (mw-double eps)) eps))
                r))
          (mw-sqrt-within
           r
           (mw-plus (mw-sqrt r (mw-double eps)) eps)
           eps))
        :name 'mw-square-root-recursive-right-half)
(assert '(implies
          (and (not (mw-large-tolerance r eps))
               (not
                (mw-le
                 (mw-square
                  (mw-plus (mw-sqrt r (mw-double eps)) eps))
                 r)))
          (mw-sqrt-within r (mw-sqrt r (mw-double eps)) eps))
        :name 'mw-square-root-recursive-left-half)
(prove '(mw-sqrt-within r ?answer eps)
       :answer '(values ?answer))
\end{lstlisting}
Answer:
\begin{lstlisting}
(ANSWER-IF (MW-LARGE-TOLERANCE R EPS)
  (VALUES ZERO)
  (ANSWER-IF
    (MW-LE
      (MW-SQUARE
        (MW-PLUS (MW-SQRT R (MW-DOUBLE EPS)) EPS))
      R)
    (VALUES (MW-PLUS (MW-SQRT R (MW-DOUBLE EPS)) EPS))
    (VALUES (MW-SQRT R (MW-DOUBLE EPS)))))
\end{lstlisting}

\subsection{MW Greatest Common Divisor}

For positive integers, the subtractive Euclidean split is:
\[
  a=b \Rightarrow g=a,\quad
  a>b \Rightarrow g=\mathrm{gcd}(a-b,b),\quad
  b>a \Rightarrow g=\mathrm{gcd}(a,b-a).
\]
SNARK input:
\begin{lstlisting}
(assert '(or (mw-eq a b) (mw-gt a b) (mw-gt b a))
        :name 'mw-gcd-trichotomy)
(assert '(implies
          (mw-eq a b)
          (mw-gcd-out a b a))
        :name 'mw-gcd-equal-case)
(assert '(implies
          (mw-gt a b)
          (mw-gcd-out a b (mw-gcd (mw-minus a b) b)))
        :name 'mw-gcd-left-reduction)
(assert '(implies
          (mw-gt b a)
          (mw-gcd-out a b (mw-gcd a (mw-minus b a))))
        :name 'mw-gcd-right-reduction)
(prove '(mw-gcd-out a b ?answer)
       :answer '(values ?answer))
\end{lstlisting}
Answer:
\begin{lstlisting}
(ANSWER-IF (MW-GT B A)
  (VALUES (MW-GCD A (MW-MINUS B A)))
  (ANSWER-IF (MW-GT A B)
    (VALUES (MW-GCD (MW-MINUS A B) B))
    (VALUES A)))
\end{lstlisting}

\subsection{MW Quotient and Remainder}

For natural \(n\) and positive \(d\), return \(q,r\) with
\(n=qd+r\) and \(0\leq r<d\).  The recursive split is:
\[
  n<d \Rightarrow (q,r)=(0,n),\quad
  n\geq d \Rightarrow
  (q,r)=(1+q',r')
\]
where \((q',r')=\mathrm{qr}(n-d,d)\).  SNARK input:
\begin{lstlisting}
(assert '(implies
          (mw-lt n d)
          (mw-qr-out n d (mw-pair zero n)))
        :name 'mw-qr-base-case)
(assert '(implies
          (not (mw-lt n d))
          (mw-qr-out
           n
           d
           (mw-pair
            (mw-succ (mw-quotient-of (mw-qr (mw-minus n d) d)))
            (mw-remainder-of (mw-qr (mw-minus n d) d)))))
        :name 'mw-qr-recursive-case)
(prove '(mw-qr-out n d ?answer)
       :answer '(values ?answer))
\end{lstlisting}
Answer:
\begin{lstlisting}
(ANSWER-IF (MW-LT N D)
  (VALUES (MW-PAIR ZERO N))
  (VALUES
    (MW-PAIR
      (MW-SUCC (MW-QUOTIENT-OF (MW-QR (MW-MINUS N D) D)))
      (MW-REMAINDER-OF (MW-QR (MW-MINUS N D) D)))))
\end{lstlisting}

\subsection{New Stable Merge}

For nonempty sorted lists \(x::xs\) and \(y::ys\), stability chooses the left
head on equality:
\[
  x\leq y \Rightarrow m=x::\mathrm{merge}(xs,y::ys),\quad
  \neg(x\leq y) \Rightarrow m=y::\mathrm{merge}(x::xs,ys).
\]
SNARK input:
\begin{lstlisting}
(assert '(implies
          (new-le x y)
          (new-merge-out
           (new-cons x xs)
           (new-cons y ys)
           (new-cons x (new-merge xs (new-cons y ys)))))
        :name 'new-merge-take-left)
(assert '(implies
          (not (new-le x y))
          (new-merge-out
           (new-cons x xs)
           (new-cons y ys)
           (new-cons y (new-merge (new-cons x xs) ys))))
        :name 'new-merge-take-right)
(prove '(new-merge-out (new-cons x xs) (new-cons y ys) ?answer)
       :answer '(values ?answer))
\end{lstlisting}
Answer:
\begin{lstlisting}
(ANSWER-IF (NEW-LE X Y)
  (VALUES (NEW-CONS X (NEW-MERGE XS (NEW-CONS Y YS))))
  (VALUES (NEW-CONS Y (NEW-MERGE (NEW-CONS X XS) YS))))
\end{lstlisting}

\subsection{New Stable Partition}

For \(x::xs\), recursively partition the tail and put \(x\) in the first or
second component depending on the predicate:
\[
  P(x) \Rightarrow (k,d)=(x::k',d'),\quad
  \neg P(x) \Rightarrow (k,d)=(k',x::d')
\]
where \((k',d')=\mathrm{partition}(xs)\).  SNARK input:
\begin{lstlisting}
(assert '(implies
          (new-keeps x)
          (new-partition-out
           (new-cons x xs)
           (new-pair
            (new-cons x (new-left (new-partition xs)))
            (new-right (new-partition xs)))))
        :name 'new-partition-keep)
(assert '(implies
          (not (new-keeps x))
          (new-partition-out
           (new-cons x xs)
           (new-pair
            (new-left (new-partition xs))
            (new-cons x (new-right (new-partition xs))))))
        :name 'new-partition-drop)
(prove '(new-partition-out (new-cons x xs) ?answer)
       :answer '(values ?answer))
\end{lstlisting}
Answer:
\begin{lstlisting}
(ANSWER-IF (NEW-KEEPS X)
  (VALUES
    (NEW-PAIR
      (NEW-CONS X (NEW-LEFT (NEW-PARTITION XS)))
      (NEW-RIGHT (NEW-PARTITION XS))))
  (VALUES
    (NEW-PAIR
      (NEW-LEFT (NEW-PARTITION XS))
      (NEW-CONS X (NEW-RIGHT (NEW-PARTITION XS))))))
\end{lstlisting}

\subsection{New IF-Expression Simplifier}

Boyer and Moore use IF-expressions as a central datatype in the tautology
checker development of \emph{A Computational Logic} \cite{BoyerMoore1979}.
Here an expression is either \(\mathsf{True}\), \(\mathsf{False}\), a variable,
or \(\mathsf{IF}(x,t,e)\).  The normal-form invariant says that every IF
condition is a variable and that there are no immediate redundancies of the
form \(\mathsf{IF}(x,A,A)\).  The stronger simplifier carries a path
environment \(\Gamma\) so that no variable is repeated along any branch.  Let
\[
  T_x=S(t,\Gamma[x:=\mathsf{true}]),
  \qquad
  E_x=S(e,\Gamma[x:=\mathsf{false}]).
\]
Then the recursive IF-node cases are:
\begin{align*}
  \Gamma(x)=\mathsf{true}
    &\Rightarrow S(\mathsf{IF}(x,t,e),\Gamma)=S(t,\Gamma),\\
  \Gamma(x)=\mathsf{false}
    &\Rightarrow S(\mathsf{IF}(x,t,e),\Gamma)=S(e,\Gamma),\\
  x\notin\Gamma,\; T_x=E_x
    &\Rightarrow S(\mathsf{IF}(x,t,e),\Gamma)=T_x,\\
  x\notin\Gamma,\; T_x\neq E_x
    &\Rightarrow S(\mathsf{IF}(x,t,e),\Gamma)=\mathsf{IF}(x,T_x,E_x).
\end{align*}
The executable interpretation also handles the leaf cases:
\(\mathsf{True}\) and \(\mathsf{False}\) return themselves; a variable already
fixed by \(\Gamma\) becomes the corresponding truth value; an unknown variable
returns itself.  The SNARK proof below isolates the recursive IF-node case.
SNARK input:
\begin{lstlisting}
(assert '(new-if-expr expr)
        :name 'new-if-input-is-if-node)
(assert '(not (and (new-assumed-true (new-condition expr) env)
                   (new-assumed-false (new-condition expr) env)))
        :name 'new-if-assumptions-consistent)
(assert '(implies
          (and (new-if-expr expr)
               (new-assumed-true (new-condition expr) env))
          (new-if-simplify-out
           expr
           env
           (new-simplify-if (new-then-branch expr) env)))
        :name 'new-if-condition-known-true)
(assert '(implies
          (and (new-if-expr expr)
               (not (new-assumed-true (new-condition expr) env))
               (new-assumed-false (new-condition expr) env))
          (new-if-simplify-out
           expr
           env
           (new-simplify-if (new-else-branch expr) env)))
        :name 'new-if-condition-known-false)
(assert '(implies
          (and (new-if-expr expr)
               (not (new-assumed-true (new-condition expr) env))
               (not (new-assumed-false (new-condition expr) env))
               (new-same
                (new-simplify-if
                 (new-then-branch expr)
                 (new-extend-true (new-condition expr) env))
                (new-simplify-if
                 (new-else-branch expr)
                 (new-extend-false (new-condition expr) env))))
          (new-if-simplify-out
           expr
           env
           (new-simplify-if
            (new-then-branch expr)
            (new-extend-true (new-condition expr) env))))
        :name 'new-if-redundant-branches)
(assert '(implies
          (and (new-if-expr expr)
               (not (new-assumed-true (new-condition expr) env))
               (not (new-assumed-false (new-condition expr) env))
               (not
                (new-same
                 (new-simplify-if
                  (new-then-branch expr)
                  (new-extend-true (new-condition expr) env))
                 (new-simplify-if
                  (new-else-branch expr)
                  (new-extend-false (new-condition expr) env)))))
          (new-if-simplify-out
           expr
           env
           (new-if
            (new-condition expr)
            (new-simplify-if
             (new-then-branch expr)
             (new-extend-true (new-condition expr) env))
            (new-simplify-if
             (new-else-branch expr)
             (new-extend-false (new-condition expr) env)))))
        :name 'new-if-rebuild)
(prove '(new-if-simplify-out expr env ?answer)
       :answer '(values ?answer))
\end{lstlisting}
Answer:
\begin{lstlisting}
(ANSWER-IF (NEW-ASSUMED-FALSE (NEW-CONDITION EXPR) ENV)
  (VALUES (NEW-SIMPLIFY-IF (NEW-ELSE-BRANCH EXPR) ENV))
  (ANSWER-IF (NEW-ASSUMED-TRUE (NEW-CONDITION EXPR) ENV)
    (VALUES (NEW-SIMPLIFY-IF (NEW-THEN-BRANCH EXPR) ENV))
    (ANSWER-IF
      (NEW-SAME
        (NEW-SIMPLIFY-IF
          (NEW-THEN-BRANCH EXPR)
          (NEW-EXTEND-TRUE (NEW-CONDITION EXPR) ENV))
        (NEW-SIMPLIFY-IF
          (NEW-ELSE-BRANCH EXPR)
          (NEW-EXTEND-FALSE (NEW-CONDITION EXPR) ENV)))
      (VALUES
        (NEW-SIMPLIFY-IF
          (NEW-THEN-BRANCH EXPR)
          (NEW-EXTEND-TRUE (NEW-CONDITION EXPR) ENV)))
      (VALUES
        (NEW-IF
          (NEW-CONDITION EXPR)
          (NEW-SIMPLIFY-IF
            (NEW-THEN-BRANCH EXPR)
            (NEW-EXTEND-TRUE (NEW-CONDITION EXPR) ENV))
          (NEW-SIMPLIFY-IF
            (NEW-ELSE-BRANCH EXPR)
            (NEW-EXTEND-FALSE (NEW-CONDITION EXPR) ENV)))))))
\end{lstlisting}

\subsection{Strict MST FOL Layer}

The non-probabilistic MST spine is now developed below the earlier prelude
predicates.  Finite sets/lists, paths, cuts, components, acyclicity, and weight
comparison are represented by first-order certificate predicates.  For example,
graph edge membership is not asserted as a high-level MST fact; it is derived
from membership in the finite graph edge list:
\begin{lstlisting}
(assert '(mstf-edges-of g edges)
        :name 'mstf-graph-edge-list)
(assert '(mstf-finite-list edges)
        :name 'mstf-finite-edge-list)
(assert '(mstf-member edge edges)
        :name 'mstf-edge-member-certificate)
(assert '(implies
          (and (mstf-edges-of g edges)
               (mstf-finite-list edges)
               (mstf-member edge edges))
          (mstf-in-graph g edge))
        :name 'mstf-input-edge-definition)
(prove '(mstf-input-edge-out g edge ?answer)
       :answer '(values ?answer))
\end{lstlisting}
Answer:
\begin{lstlisting}
(VALUES EDGE)
\end{lstlisting}

Reachability is likewise represented by finite path certificates:
\begin{lstlisting}
(assert '(mstf-path-list path)
        :name 'mstf-path-is-list)
(assert '(mstf-path-connects path u v)
        :name 'mstf-path-endpoints)
(assert '(mstf-path-edges-in-tree path tree)
        :name 'mstf-path-tree-certificate)
(assert '(implies
          (and (mstf-path-list path)
               (mstf-path-connects path u v)
               (mstf-path-edges-in-tree path tree))
          (mstf-reaches tree u v path))
        :name 'mstf-reachability-definition)
(prove '(mstf-path-out tree u v path ?answer)
       :answer '(values ?answer))
\end{lstlisting}
Answer:
\begin{lstlisting}
(VALUES PATH)
\end{lstlisting}

The cut and finite-weight layers provide the data needed by the exchange
argument:
\begin{lstlisting}
(CUT-CROSSING-FROM-ENDPOINTS
  (VALUES EDGE))

(WEIGHT-COMPARISON
  (VALUES EDGE))

(LIGHTEST-CROSSING-FROM-FINITE-COMPARISONS
  (VALUES EDGE))
\end{lstlisting}
Acyclicity is supplied by bridge-cut certificates for every selected tree
edge, and spanning trees are then derived from graph-edge inclusion, finite
path certificates for all connected pairs, and those bridge certificates:
\begin{lstlisting}
(ACYCLIC-FROM-BRIDGE-CUTS
  (VALUES TREE))

(SPANNING-TREE-FROM-CERTIFICATES
  (VALUES TREE))
\end{lstlisting}

The exchange theorem is now stated over the lower path/cut/weight facts:
\begin{lstlisting}
(EXCHANGE-FROM-PATH-CUT-WEIGHT
  (VALUES (MSTF-EXCHANGE TREE OLD EDGE)))
\end{lstlisting}
The blue and red rules no longer take \texttt{safe-edge} or
\texttt{heaviest-on-cycle} as opaque MST obligations.  They consume finite
path/cut/cycle/weight evidence and extract:
\begin{lstlisting}
(BLUE-RULE-FROM-EXCHANGE
  (VALUES
    (MSTF-EXCHANGE TREE (MSTF-CROSSING-EDGE PATH CUT) EDGE)))

(RED-RULE-FROM-CYCLE
  (VALUES (MSTF-DELETE-EDGE EDGE G)))
\end{lstlisting}
Finally, the ordinary algorithms are consequences of these lemmas:
\begin{lstlisting}
(BATCH-SAFETY-FROM-COMPONENT-CUTS
  (VALUES (MSTF-ADD-BATCH BATCH FOREST)))

(KRUSKAL-FROM-BLUE-RULE
  (VALUES (MSTF-KRUSKAL-STEP G (MSTF-ADD-EDGE EDGE FOREST))))

(PRIM-FROM-BLUE-RULE
  (VALUES (MSTF-PRIM-STEP G (MSTF-ADD-EDGE EDGE FOREST) ROOT)))

(BORUVKA-FROM-BATCH-SAFETY
  (VALUES (MSTF-BORUVKA-PHASE G (MSTF-CONTRACT-SET G BATCH))))
\end{lstlisting}

\subsection{MST Prelude: Reified FOL Objects}

The broader MST development still uses a prelude.  Since ordinary FOL does not
contain transitive closure, set summation, probability spaces, heaps, or
distributed schedules as built-ins, the prelude treats each as a first-order
object.  The main reified concepts are:
\begin{lstlisting}
paths, cycles, cuts, finite edge sets, exchange trees
contractions, weight comparisons, samples, heap states
matroids, update streams, distributed message schedules
\end{lstlisting}
Definitions are then stated over those objects.  For example, a spanning tree
is exposed through three first-order predicates:
\begin{lstlisting}
(assert '(mstp-all-input-edges g tree)
        :name 'mstp-input-edges-premise)
(assert '(mstp-spans g tree)
        :name 'mstp-spans-premise)
(assert '(mstp-acyclic tree)
        :name 'mstp-acyclic-premise)
(assert '(implies
          (and (mstp-all-input-edges g tree)
               (mstp-spans g tree)
               (mstp-acyclic tree))
          (mstp-spanning-tree g tree))
        :name 'mstp-spanning-tree-definition)
(prove '(mstp-spanning-tree-out g tree ?answer)
       :answer '(values ?answer))
\end{lstlisting}
Answer:
\begin{lstlisting}
(VALUES TREE)
\end{lstlisting}

The MST definition likewise keeps competitors as first-order tree objects:
\begin{lstlisting}
(assert '(mstp-spanning-tree g tree)
        :name 'mstp-mst-spanning-premise)
(assert '(mstp-competitor-no-lighter g tree other)
        :name 'mstp-mst-competitor-premise)
(assert '(implies
          (and (mstp-spanning-tree g tree)
               (mstp-competitor-no-lighter g tree other))
          (mstp-mst g tree))
        :name 'mstp-mst-definition)
(prove '(mstp-mst-out g tree ?answer)
       :answer '(values ?answer))
\end{lstlisting}
Answer:
\begin{lstlisting}
(VALUES TREE)
\end{lstlisting}

\subsection{MST Prelude: Exchange and Cut Lemmas}

The prelude derives the witness terms used by the blue/cut rule.  A reified
tree path crossing a cut has a selected crossing edge:
\begin{lstlisting}
(prove '(mstp-crossing-witness-out tree path cut ?answer)
       :answer '(values ?answer))
\end{lstlisting}
Answer:
\begin{lstlisting}
(VALUES (MSTP-CROSSING-EDGE PATH CUT))
\end{lstlisting}
The spanning and weight halves of the exchange argument extract:
\begin{lstlisting}
(EXCHANGE-SPANNING-LEMMA
  (VALUES (MSTP-EXCHANGE TREE OLD EDGE)))

(EXCHANGE-WEIGHT-LEMMA
  (VALUES (MSTP-EXCHANGE TREE OLD EDGE)))
\end{lstlisting}
Assembling those gives the blue-rule witness:
\begin{lstlisting}
(BLUE-RULE-LEMMA
  (VALUES
    (MSTP-EXCHANGE TREE (MSTP-CROSSING-EDGE PATH CUT) EDGE)))
\end{lstlisting}
The dual red/cycle rule produces the deletion program:
\begin{lstlisting}
(RED-RULE-LEMMA
  (VALUES (MSTP-DELETE-EDGE EDGE G)))
\end{lstlisting}

\subsection{MST Prelude: Reduction and Certification Lemmas}

The remaining prelude lemmas produce the pseudo-code terms consumed by the MST
algorithm layer:
\begin{lstlisting}
(CONTRACT-SAFE-LEMMA
  (VALUES (MSTP-LIFT EDGE TREE)))

(DELETE-USELESS-LEMMA
  (VALUES (MSTP-DELETE-EDGE EDGE G)))

(BORUVKA-BATCH-LEMMA
  (VALUES (MSTP-LIFT BATCH TREE)))

(VERIFICATION-LEMMA
  (VALUES (MSTP-VERIFY-TREE G TREE)))

(REPLACEMENT-EDGE-LEMMA
  (VALUES (MSTP-RECORD-REPLACEMENT EDGE REPLACEMENT)))

(SECOND-BEST-LEMMA
  (VALUES (MSTP-SECOND-BEST-FROM-REPLACEMENTS TREE REPLACEMENT)))

(F-HEAVY-FILTER-LEMMA
  (VALUES (MSTP-FILTER-HEAVY G SAMPLE-TREE)))

(KKT-FILTER-LEMMA
  (VALUES (MSTP-KKT FILTERED RANDOM-SOURCE)))

(SOFT-HEAP-REPAIR-LEMMA
  (VALUES
    (MSTP-REPAIR-CORRUPTIONS G
      (MSTP-SOFT-HEAP-CANDIDATES G EPSILON))))

(MATROID-GREEDY-LEMMA
  (VALUES (MSTP-MATROID-GREEDY MATROID ORDER)))

(DYNAMIC-LEMMA
  (VALUES (MSTP-DYNAMIC-SEQUENCE G UPDATES)))

(GHS-LEMMA
  (VALUES (MSTP-GHS-PHASE G FRAGMENTS)))
\end{lstlisting}
The algorithm layer then consumes these lemma terms.  For example:
\begin{lstlisting}
(REVERSE-DELETE-ALGORITHM
  (VALUES (MSTP-REVERSE-DELETE (MSTP-DELETE-EDGE EDGE G))))

(KRUSKAL-ALGORITHM
  (VALUES (MSTP-KRUSKAL-STEP G (MSTP-ADD-EDGE EDGE FOREST))))

(PRIM-ALGORITHM
  (VALUES (MSTP-PRIM-STEP G (MSTP-ADD-EDGE EDGE FOREST) U)))

(BORUVKA-ALGORITHM
  (VALUES (MSTP-BORUVKA-PHASE G (MSTP-CONTRACT-SET G BATCH))))

(FILTER-KRUSKAL-ALGORITHM
  (VALUES (MSTP-FILTER-KRUSKAL FILTERED)))
\end{lstlisting}

\subsection{New MST: Abstract Specification}

The MST development reifies sets, cuts, forests, batches, and strategies as
first-order objects.  The second-order comparison against every candidate tree
is represented by first-order predicates over tree objects, including
\texttt{NEW-SPANNING-TREE} and \texttt{NEW-MINIMUM-TOTAL-WEIGHT}.  At the top
level:
\[
  \mathrm{MST}(G,T) \equiv
  \mathrm{SpanningTree}(G,T)\land \mathrm{MinimumTotalWeight}(G,T).
\]
SNARK input:
\begin{lstlisting}
(assert '(new-spanning-tree g tree)
        :name 'new-mst-spanning-tree-premise)
(assert '(new-minimum-total-weight g tree)
        :name 'new-mst-minimality-premise)
(assert '(implies
          (and (new-spanning-tree g tree)
               (new-minimum-total-weight g tree))
          (new-mst-out g tree tree))
        :name 'new-mst-specification)
(prove '(new-mst-out g tree ?answer)
       :answer '(values ?answer))
\end{lstlisting}
Answer:
\begin{lstlisting}
(VALUES TREE)
\end{lstlisting}

\subsection{New MST: Abstract Greedy Skeleton}

The first algorithmic refinement is deliberately not Kruskal, Prim, or Boruvka.
It states only the shared greedy invariant:
\begin{align*}
  \mathrm{complete}(G,F) &\Rightarrow A=F,\\
  \neg\mathrm{complete}(G,F) &\Rightarrow
    A=\mathrm{Greedy}(G,\mathrm{Extend}(G,F)).
\end{align*}
SNARK input:
\begin{lstlisting}
(assert '(implies
          (new-mst-complete g forest)
          (new-mst-greedy-out g forest forest))
        :name 'new-mst-greedy-finished)
(assert '(implies
          (not (new-mst-complete g forest))
          (new-mst-greedy-out
           g
           forest
           (new-mst-greedy g (new-mst-extend g forest))))
        :name 'new-mst-greedy-continues)
(prove '(new-mst-greedy-out g forest ?answer)
       :answer '(values ?answer))
\end{lstlisting}
Answer:
\begin{lstlisting}
(ANSWER-IF (NEW-MST-COMPLETE G FOREST)
  (VALUES FOREST)
  (VALUES (NEW-MST-GREEDY G (NEW-MST-EXTEND G FOREST))))
\end{lstlisting}

\subsection{New MST: Safe Extension}

The next refinement exposes the safe-extension point.  A Boruvka-style batch of
safe edges may be added at once; otherwise the refinement adds one safe edge:
\[
  \mathrm{SafeBatch}(G,F,B)\Rightarrow A=F+B,\qquad
  \mathrm{SafeEdge}(G,F,e)\Rightarrow A=F+e.
\]
SNARK input:
\begin{lstlisting}
(assert '(or (new-safe-batch g forest batch)
             (new-safe-edge g forest edge))
        :name 'new-mst-extension-kind)
(assert '(implies
          (new-safe-batch g forest batch)
          (new-mst-extension-out
           g
           forest
           (new-mst-add-batch batch forest)))
        :name 'new-mst-safe-batch-extension)
(assert '(implies
          (and (not (new-safe-batch g forest batch))
               (new-safe-edge g forest edge))
          (new-mst-extension-out
           g
           forest
           (new-mst-add-edge edge forest)))
        :name 'new-mst-safe-edge-extension)
(prove '(new-mst-extension-out g forest ?answer)
       :answer '(values ?answer))
\end{lstlisting}
Answer:
\begin{lstlisting}
(ANSWER-IF (NEW-SAFE-EDGE G FOREST EDGE)
  (ANSWER-IF (NEW-SAFE-BATCH G FOREST BATCH)
    (VALUES (NEW-MST-ADD-BATCH BATCH FOREST))
    (VALUES (NEW-MST-ADD-EDGE EDGE FOREST)))
  (VALUES (NEW-MST-ADD-BATCH BATCH FOREST)))
\end{lstlisting}

\subsection{New MST: Cut Property}

The cut-property refinement says that a lightest edge crossing a cut respected
by the current forest is a safe single-edge extension:
\[
  \mathrm{RespectedCut}(G,F,C)\land
  \mathrm{Crosses}(e,C)\land
  \mathrm{LightestCrossing}(G,e,C)
  \Rightarrow A=F+e.
\]
SNARK input:
\begin{lstlisting}
(assert '(new-cut-respected g forest cut)
        :name 'new-mst-cut-respected-premise)
(assert '(new-crosses-cut edge cut)
        :name 'new-mst-cut-crossing-premise)
(assert '(new-lightest-crossing g edge cut)
        :name 'new-mst-cut-lightest-premise)
(assert '(implies
          (and (new-cut-respected g forest cut)
               (new-crosses-cut edge cut)
               (new-lightest-crossing g edge cut))
          (new-mst-extension-out
           g
           forest
           (new-mst-add-edge edge forest)))
        :name 'new-mst-cut-property-extension)
(prove '(new-mst-extension-out g forest ?answer)
       :answer '(values ?answer))
\end{lstlisting}
Answer:
\begin{lstlisting}
(VALUES (NEW-MST-ADD-EDGE EDGE FOREST))
\end{lstlisting}

\subsection{New MST: Kruskal, Prim, and Boruvka}

Kruskal, Prim/Jarnik, and Boruvka are encoded as specializations of the same
safe-extension relation:
\begin{align*}
  \mathrm{KruskalEligible}(G,F,e)\land\mathrm{KruskalLightest}(G,F,e)
    &\Rightarrow A=F+e,\\
  \mathrm{PrimFrontier}(G,F,r,e)\land\mathrm{PrimLightest}(G,F,r,e)
    &\Rightarrow A=F+e,\\
  \mathrm{BoruvkaBatch}(G,F,B)
    &\Rightarrow A=F+B.
\end{align*}
Representative SNARK input for the Kruskal refinement:
\begin{lstlisting}
(assert '(new-kruskal-eligible g forest edge)
        :name 'new-mst-kruskal-eligible-premise)
(assert '(new-kruskal-lightest g forest edge)
        :name 'new-mst-kruskal-lightest-premise)
(assert '(implies
          (and (new-kruskal-eligible g forest edge)
               (new-kruskal-lightest g forest edge))
          (new-mst-extension-out
           g
           forest
           (new-mst-add-edge edge forest)))
        :name 'new-mst-kruskal-extension)
(prove '(new-mst-extension-out g forest ?answer)
       :answer '(values ?answer))
\end{lstlisting}
The extracted answers for the three refinements are:
\begin{lstlisting}
(KRUSKAL-REFINEMENT
  (VALUES (NEW-MST-ADD-EDGE EDGE FOREST)))

(PRIM-REFINEMENT
  (VALUES (NEW-MST-ADD-EDGE EDGE FOREST)))

(BORUVKA-REFINEMENT
  (VALUES (NEW-MST-ADD-BATCH BATCH FOREST)))
\end{lstlisting}

\subsection{New MST: Strategy Dispatch and Executable Leaves}

The final symbolic layer dispatches among the concrete refinements:
\begin{lstlisting}
(assert '(or (new-strategy-kruskal strategy)
             (new-strategy-prim strategy)
             (new-strategy-boruvka strategy))
        :name 'new-mst-strategy-cases)
(prove '(new-mst-strategy-out g forest strategy ?answer)
       :answer '(values ?answer))
\end{lstlisting}
The resulting proof answer is a nested \texttt{ANSWER-IF} over the strategy
predicates.  SNARK does not simplify all impossible combinations away in the
answer term; the executable layer supplies the concrete dispatch:
\begin{lstlisting}
(new-run-mst graph :strategy :kruskal)
(new-run-mst graph :strategy :prim)
(new-run-mst graph :strategy :boruvka)
\end{lstlisting}
On the sample finite graph, all three concrete interpretations return total
weight \(9\), though the edge order and tie choices differ.

\subsection{Advanced MST: Red Rule and Reverse Delete}

The dual of the cut rule is the red rule: a heaviest edge on a cycle can be
discarded without changing the existence of an MST.  As a one-step refinement:
\[
  \mathrm{HeaviestOnCycle}(G,e,C)\Rightarrow G'=G-e.
\]
SNARK input:
\begin{lstlisting}
(assert '(new-heaviest-on-cycle g edge cycle)
        :name 'new-mst-red-cycle-premise)
(assert '(implies
          (new-heaviest-on-cycle g edge cycle)
          (new-mst-red-rule-out
           g
           edge
           (new-mst-delete-edge edge g)))
        :name 'new-mst-red-rule)
(prove '(new-mst-red-rule-out g edge ?answer)
       :answer '(values ?answer))
\end{lstlisting}
Answer:
\begin{lstlisting}
(VALUES (NEW-MST-DELETE-EDGE EDGE G))
\end{lstlisting}

Reverse-delete repeatedly applies this deletion test in descending weight
order, preserving an edge exactly when its removal would disconnect the graph:
\begin{lstlisting}
(assert '(or (new-still-spans-after-delete g edge)
             (not (new-still-spans-after-delete g edge)))
        :name 'new-mst-reverse-delete-cases)
(prove '(new-mst-reverse-delete-out g edge ?answer)
       :answer '(values ?answer))
\end{lstlisting}
Answer:
\begin{lstlisting}
(ANSWER-IF (NEW-STILL-SPANS-AFTER-DELETE G EDGE)
  (VALUES (NEW-MST-REVERSE-DELETE (NEW-MST-DELETE-EDGE EDGE G)))
  (VALUES (NEW-MST-REVERSE-DELETE G)))
\end{lstlisting}

\subsection{Advanced MST: Contract/Delete Normalization}

The red and blue rules combine into a graph-normalization refinement.  Safe
edges may be contracted; useless edges may be deleted.  The implementation
therefore exposes a normalizing branch before choosing a concrete algorithm:
\begin{lstlisting}
(assert '(or (new-safe-edge g forest edge)
             (new-useless-edge g forest edge))
        :name 'new-mst-contract-delete-cases)
(prove '(new-mst-contract-delete-out g forest edge ?answer)
       :answer '(values ?answer))
\end{lstlisting}
Answer:
\begin{lstlisting}
(ANSWER-IF (NEW-USELESS-EDGE G FOREST EDGE)
  (ANSWER-IF (NEW-SAFE-EDGE G FOREST EDGE)
    (VALUES (NEW-MST-CONTRACT-EDGE EDGE G))
    (VALUES (NEW-MST-DELETE-EDGE EDGE G)))
  (VALUES (NEW-MST-CONTRACT-EDGE EDGE G)))
\end{lstlisting}
The last branch is logically unreachable under the asserted disjunction unless
the safe-edge case holds.  This is a useful example of the gap between a proof
answer and a cleaned host-language program: the executable layer may simplify
the branch once the case split is interpreted.

\subsection{Advanced MST: Boruvka Phase}

The base MST file had a Boruvka leaf.  The advanced file records the full phase
shape: each component chooses a minimum outgoing edge, the whole batch is safe,
the graph is contracted, and the recursive result is lifted:
\begin{lstlisting}
(assert '(new-components-cover g forest)
        :name 'new-mst-boruvka-components-premise)
(assert '(new-each-component-lightest-outgoing g forest batch)
        :name 'new-mst-boruvka-lightest-premise)
(prove '(new-mst-boruvka-phase-out g forest ?answer)
       :answer '(values ?answer))
\end{lstlisting}
Answer:
\begin{lstlisting}
(VALUES
  (NEW-MST-LIFT BATCH
    (NEW-MST-GREEDY
      (NEW-MST-CONTRACT-SET G BATCH)
      (NEW-MST-CONTRACT-FOREST FOREST BATCH))))
\end{lstlisting}

\subsection{Advanced MST: Kruskal Variants}

Kruskal-style algorithms share the same logical filter, but differ in how they
realize the scan: sorted edge list, union-find scan, or Filter-Kruskal
\cite{Kruskal1956,CheritonTarjan1976,OsipovSandersSingler2009}.  SNARK sees
these as policy refinements:
\begin{lstlisting}
(assert '(or (new-kruskal-policy-sorted policy)
             (new-kruskal-policy-union-find policy)
             (new-kruskal-policy-filter policy))
        :name 'new-mst-kruskal-policy-cases)
(prove '(new-mst-kruskal-variant-out g policy ?answer)
       :answer '(values ?answer))
\end{lstlisting}
Answer:
\begin{lstlisting}
(ANSWER-IF (NEW-KRUSKAL-POLICY-FILTER POLICY)
  (ANSWER-IF (NEW-KRUSKAL-POLICY-UNION-FIND POLICY)
    (VALUES (NEW-MST-UNION-FIND-SCAN G))
    (ANSWER-IF (NEW-KRUSKAL-POLICY-SORTED POLICY)
      (VALUES (NEW-MST-SORTED-SCAN G))
      (VALUES (NEW-MST-FILTER-KRUSKAL G))))
  (ANSWER-IF (NEW-KRUSKAL-POLICY-SORTED POLICY)
    (ANSWER-IF (NEW-KRUSKAL-POLICY-UNION-FIND POLICY)
      (VALUES (NEW-MST-UNION-FIND-SCAN G))
      (VALUES (NEW-MST-SORTED-SCAN G)))
    (VALUES (NEW-MST-UNION-FIND-SCAN G))))
\end{lstlisting}

\subsection{Advanced MST: Prim Queue Refinements}

Prim/Jarnik admits the same kind of policy refinement.  The mathematical choice
is still the lightest frontier edge; the implementation policy chooses a dense
array scan, binary heap, Fibonacci heap, or pairing heap.  The Fibonacci-heap
case connects to the Fredman--Tarjan network-optimization development
\cite{FredmanTarjan1987}.
SNARK input:
\begin{lstlisting}
(assert '(or (new-prim-policy-dense policy)
             (new-prim-policy-binary-heap policy)
             (new-prim-policy-fibonacci-heap policy)
             (new-prim-policy-pairing-heap policy))
        :name 'new-mst-prim-policy-cases)
(prove '(new-mst-prim-variant-out g root policy ?answer)
       :answer '(values ?answer))
\end{lstlisting}
Answer:
\begin{lstlisting}
(ANSWER-IF (NEW-PRIM-POLICY-DENSE POLICY)
  (VALUES (NEW-MST-DENSE-PRIM G ROOT))
  (VALUES (NEW-MST-HEAP-PRIM G ROOT QUEUE)))
\end{lstlisting}

\subsection{Advanced MST: Verification}

The verification theorem turns MST construction into a certifier.  A tree is
minimum if it spans, is acyclic, and every non-tree edge is at least as heavy
as the maximum edge on the corresponding tree path
\cite{DixonRauchTarjan1992}:
\begin{lstlisting}
(assert '(new-tree-spans g tree)
        :name 'new-mst-verify-spans-premise)
(assert '(new-tree-acyclic g tree)
        :name 'new-mst-verify-acyclic-premise)
(assert '(new-path-max-dominates-nontree-edges g tree)
        :name 'new-mst-verify-path-max-premise)
(prove '(new-mst-verifier-out g tree ?answer)
       :answer '(values ?answer))
\end{lstlisting}
Answer:
\begin{lstlisting}
(VALUES (NEW-MST-VERIFY-TREE G TREE))
\end{lstlisting}

\subsection{Advanced MST: Replacement Edges and Second Best}

Sensitivity analysis asks, for each tree edge \(e\), for the lightest
non-tree edge reconnecting the two components of \(T-e\).  This supports
replacement-edge tables, second-best MSTs, and dynamic-update certificates
\cite{DixonRauchTarjan1992,Eppstein1994}.
SNARK input:
\begin{lstlisting}
(assert '(new-tree-edge tree edge)
        :name 'new-mst-replacement-tree-edge-premise)
(assert '(new-min-replacement-edge g tree edge replacement)
        :name 'new-mst-replacement-min-premise)
(prove '(new-mst-replacement-out g tree ?answer)
       :answer '(values ?answer))
\end{lstlisting}
Answer:
\begin{lstlisting}
(VALUES (NEW-MST-RECORD-REPLACEMENT EDGE REPLACEMENT))
\end{lstlisting}
Once all replacements are certified, the next refinement chooses the least
increase over the original MST:
\begin{lstlisting}
(VALUES (NEW-MST-SECOND-BEST-FROM-REPLACEMENTS TREE REPLACEMENTS))
\end{lstlisting}

\subsection{Advanced MST: Karger--Klein--Tarjan Filtering}

The Karger--Klein--Tarjan algorithm uses random sampling, an MST of the sample,
and deletion of \(F\)-heavy edges before recursing
\cite{KargerKleinTarjan1995}.  Probability and expected-time bounds live in
the metatheory; the SNARK refinement captures the correctness-preserving
filter:
\begin{lstlisting}
(assert '(new-random-sample g random-source sample)
        :name 'new-mst-kkt-sample-premise)
(assert '(new-sample-mst sample sample-tree)
        :name 'new-mst-kkt-sample-tree-premise)
(assert '(new-f-heavy-filtered g sample-tree filtered)
        :name 'new-mst-kkt-filtered-premise)
(prove '(new-mst-kkt-out g random-source ?answer)
       :answer '(values ?answer))
\end{lstlisting}
Answer:
\begin{lstlisting}
(VALUES (NEW-MST-KKT FILTERED RANDOM-SOURCE))
\end{lstlisting}

\subsection{Advanced MST: Soft-Heap Refinement}

Chazelle's MST algorithm uses soft heaps to trade exact key order for a
controlled corruption discipline \cite{Chazelle2000}.  The SNARK version
abstracts the data structure as candidate generation plus repair/filtering:
\begin{lstlisting}
(assert '(new-soft-heap-candidate-set g epsilon batch)
        :name 'new-mst-soft-candidates-premise)
(assert '(new-corruption-repair-valid g batch filtered)
        :name 'new-mst-soft-repair-premise)
(prove '(new-mst-soft-heap-out g epsilon forest ?answer)
       :answer '(values ?answer))
\end{lstlisting}
Answer:
\begin{lstlisting}
(VALUES
  (NEW-MST-GREEDY
    (NEW-MST-REPAIR-CORRUPTIONS G
      (NEW-MST-SOFT-HEAP-CANDIDATES G EPSILON))
    FOREST))
\end{lstlisting}

\subsection{Advanced MST: Matroid Greedy}

The graphic-matroid view generalizes Kruskal: an MST is a minimum-weight basis
of the graphic matroid, and the greedy theorem for matroids explains why the
cycle test is enough \cite{Edmonds1971,PettieRamachandran2002}.
SNARK input:
\begin{lstlisting}
(assert '(new-matroid matroid)
        :name 'new-mst-matroid-premise)
(assert '(new-graphic-matroid-of g matroid)
        :name 'new-mst-graphic-matroid-premise)
(assert '(new-weight-order-for matroid weight-order)
        :name 'new-mst-weight-order-premise)
(prove '(new-mst-matroid-out g matroid weight-order ?answer)
       :answer '(values ?answer))
\end{lstlisting}
Answer:
\begin{lstlisting}
(VALUES (NEW-MST-MATROID-GREEDY MATROID WEIGHT-ORDER))
\end{lstlisting}

\subsection{Advanced MST: Dynamic and Distributed Hooks}

Two final refinements expose interfaces to the dynamic and distributed MST
literature.  Offline dynamic MST maintenance computes a sequence of MSTs from
a known update stream \cite{Eppstein1994}; GHS-style distributed MST
construction merges fragments through minimum-outgoing-edge messages
\cite{GallagerHumbletSpira1983}.
SNARK answers:
\begin{lstlisting}
(OFFLINE-DYNAMIC-REFINEMENT
  (VALUES (NEW-MST-DYNAMIC-SEQUENCE G UPDATES)))

(GHS-DISTRIBUTED-REFINEMENT
  (VALUES (NEW-MST-GHS-PHASE G FRAGMENTS)))
\end{lstlisting}

\section{Working Example I: Unification}

Manna and Waldinger's unification paper specifies unification as the task of
finding a substitution that makes two expressions identical, or reporting
failure when no such substitution exists \cite{MannaWaldinger1981}.  Later
presentations emphasize the same existential-output view and its fit with
SNARK-style proof search \cite{Waldinger2021}.  The local SNARK example uses a
small first-order term language:
\begin{lstlisting}
mw-var, mw-const, mw-app        ; expression constructors
mw-bind, mw-compose            ; substitution constructors
mw-unify, mw-subst, mw-subst-of
mw-none, mw-some               ; failure/success result constructors
\end{lstlisting}

\subsection{Variable Case}

The variable case encodes the occurs check:
\begin{align*}
  \mathrm{occurs}(\mathrm{var}(v),t)
    &\Rightarrow \mathrm{unify\_out}(\mathrm{var}(v),t,\mathrm{none}),\\
  \neg\mathrm{occurs}(\mathrm{var}(v),t)
    &\Rightarrow \mathrm{unify\_out}(\mathrm{var}(v),t,
       \mathrm{some}(\mathrm{bind}(v,t))).
\end{align*}
The query asks SNARK to find a result:
\begin{lstlisting}
(prove '(mw-unify-out (mw-var v) term ?result)
       :answer '(values ?result))
\end{lstlisting}
The extracted answer is:
\begin{lstlisting}
(ANSWER-IF (MW-OCCURS (MW-VAR V) TERM)
  (VALUES MW-NONE)
  (VALUES (MW-SOME (MW-BIND V TERM))))
\end{lstlisting}
This is the expected program fragment: fail if the occurs check fails;
otherwise return the singleton binding.

\subsection{Compound Case}

For compound terms, the classical algorithm recursively unifies the first
components, substitutes that result through the remaining components, then
unifies the remaining components and composes substitutions.  The SNARK
obligations encode three cases:
\begin{enumerate}
\item the first recursive unification fails;
\item the first succeeds but the second recursive unification fails;
\item both succeed and their substitutions are composed.
\end{enumerate}
The extracted answer is the following nested conditional:
\begin{lstlisting}
(ANSWER-IF (MW-FAIL (MW-UNIFY M M1))
  (VALUES MW-NONE)
  (ANSWER-IF
    (MW-FAIL
      (MW-UNIFY
        (MW-SUBST (MW-SUBST-OF (MW-UNIFY M M1)) N)
        (MW-SUBST (MW-SUBST-OF (MW-UNIFY M M1)) N1)))
    (VALUES MW-NONE)
    (VALUES
      (MW-SOME
        (MW-COMPOSE
          (MW-SUBST-OF (MW-UNIFY M M1))
          (MW-SUBST-OF
            (MW-UNIFY
              (MW-SUBST (MW-SUBST-OF (MW-UNIFY M M1)) N)
              (MW-SUBST (MW-SUBST-OF (MW-UNIFY M M1)) N1))))))))
\end{lstlisting}
This term mirrors the conventional recursive unification skeleton, while
remaining within SNARK's first-order answer-extraction mechanism.

\section{Working Example II: Square Root and Binary Search}

Manna and Waldinger's binary-search example starts from a specification for
approximating the square root of a nonnegative real number within a positive
tolerance.  Their derivation shows that a binary-search step emerges naturally:
given a rougher interval of width \(2\epsilon\), test whether the exact square
root is in the right half; if so, move the lower endpoint right by
\(\epsilon\), otherwise keep it \cite{MannaWaldinger1985IJCAI,MannaWaldinger1987}.
The local SNARK development abstracts away the full real
arithmetic theory and focuses on this constructive branch.

\subsection{Binary-Search Step}

The predicate \texttt{MW-SQRT-WITHIN} denotes that an answer is within the
desired interval.  The predicate \texttt{MW-LE} abstracts the comparison
\((z+\epsilon)^2 \leq r\).  The two obligations are:
\begin{align*}
  (z+\epsilon)^2 \leq r
    &\Rightarrow \mathrm{sqrt\_within}(r,z+\epsilon,\epsilon),\\
  \neg((z+\epsilon)^2 \leq r)
    &\Rightarrow \mathrm{sqrt\_within}(r,z,\epsilon).
\end{align*}
The extracted SNARK answer is:
\begin{lstlisting}
(ANSWER-IF (MW-LE (MW-SQUARE (MW-PLUS Z EPS)) R)
  (VALUES (MW-PLUS Z EPS))
  (VALUES Z))
\end{lstlisting}
This is the binary-search branch in its smallest form.

\subsection{Recursive Skeleton}

The second square-root example adds a base-case test and a recursive call
\texttt{MW-SQRT} at doubled
tolerance:
\begin{lstlisting}
(ANSWER-IF (MW-LARGE-TOLERANCE R EPS)
  (VALUES ZERO)
  (ANSWER-IF
    (MW-LE
      (MW-SQUARE
        (MW-PLUS (MW-SQRT R (MW-DOUBLE EPS)) EPS))
      R)
    (VALUES (MW-PLUS (MW-SQRT R (MW-DOUBLE EPS)) EPS))
    (VALUES (MW-SQRT R (MW-DOUBLE EPS)))))
\end{lstlisting}
This is faithful to the structural idea in the original derivation: use a
larger tolerance recursively to obtain a rough estimate, then decide which half
of the interval contains the desired value.

\section{Working Example III: Greatest Common Divisor}

Manna and Waldinger used the greatest-common-divisor program as one of their
arithmetic examples in the intermittent-assertion account of program
correctness \cite{MannaWaldinger1978}.  The local SNARK example reuses the same
subtractive Euclidean structure in synthesis form.  For positive integers
\(a\) and \(b\), one of three cases applies:
\begin{align*}
  a=b &\Rightarrow \mathrm{gcd}(a,b)=a,\\
  a>b &\Rightarrow \mathrm{gcd}(a,b)=\mathrm{gcd}(a-b,b),\\
  b>a &\Rightarrow \mathrm{gcd}(a,b)=\mathrm{gcd}(a,b-a).
\end{align*}
In SNARK, the comparison predicates and subtraction operation are kept
abstract.  The proof asks for an output satisfying \texttt{MW-GCD-OUT}:
\begin{lstlisting}
(prove '(mw-gcd-out a b ?answer)
       :answer '(values ?answer))
\end{lstlisting}
The extracted answer is:
\begin{lstlisting}
(ANSWER-IF (MW-GT B A)
  (VALUES (MW-GCD A (MW-MINUS B A)))
  (ANSWER-IF (MW-GT A B)
    (VALUES (MW-GCD (MW-MINUS A B) B))
    (VALUES A)))
\end{lstlisting}
The order of tests is chosen by SNARK's proof search, but the control
structure is the expected one: when neither strict inequality branch applies,
the equality case returns \(a\).

\section{Working Example IV: Quotient and Remainder}

The quotient/remainder example follows the integer-division development in
Manna and Waldinger's TOPLAS paper on deductive program synthesis
\cite{MannaWaldinger1980}.  The target computation is: for natural \(n\) and
positive \(d\), return \(q,r\) such that
\[
  n = qd + r \quad\hbox{and}\quad 0 \leq r < d.
\]
The constructive split is the familiar recursive-subtraction program.  If
\(n<d\), the quotient is zero and the remainder is \(n\).  Otherwise, recurse
on \(n-d\), increment the quotient component, and keep the recursive remainder.
The SNARK proof is:
\begin{lstlisting}
(prove '(mw-qr-out n d ?answer)
       :answer '(values ?answer))
\end{lstlisting}
and the extracted answer is:
\begin{lstlisting}
(ANSWER-IF (MW-LT N D)
  (VALUES (MW-PAIR ZERO N))
  (VALUES
    (MW-PAIR
      (MW-SUCC (MW-QUOTIENT-OF (MW-QR (MW-MINUS N D) D)))
      (MW-REMAINDER-OF (MW-QR (MW-MINUS N D) D)))))
\end{lstlisting}
Here again the proof term is not yet host-language code.  It names a recursive
call \texttt{MW-QR}, projections from its pair result, and arithmetic
operations that must be interpreted before the program can be run.

\section{Working Example V: MST Refinement Ladder}

The minimum-spanning-tree development is the most substantial new example in
the suite.  Unlike the small branch-extraction examples, it is organized as a
hierarchy of specifications and refinements.  The point is not to choose
Kruskal, Jarnik--Prim, or Boruvka
\cite{Kruskal1956,Jarnik1930,Prim1957,Boruvka1926} at the top.  Instead, the
development first states what an MST witness is, then introduces a generic
greedy extension operator, then refines that extension through the cut
property, and only at the leaves commits to the familiar algorithm families.
The no-shortcut version has an explicit prelude in front of this ladder:
paths, cuts, cycles, exchange trees, contractions, finite weight comparisons,
samples, heaps, matroids, update streams, and message schedules are all
first-order objects, and the algorithmic rules consume the lemma terms
extracted from that prelude.

\subsection{Reifying the Specification}

The mathematical specification supplied by the user has the right high-level
shape:
\[
  \mathrm{MST}(G,T) \equiv
  \mathrm{SpanningTree}(G,T)\land
  \mathrm{MinimumWeight}(G,T).
\]
Here the minimum-weight clause abbreviates:
\[
  \mathrm{MinimumWeight}(G,T) \equiv
  \forall S.\,(\mathrm{SpanningTree}(G,S)\Rightarrow
  \mathrm{WeightLeq}(T,S)).
\]
To keep the SNARK development first-order, candidate trees, cuts, forests,
batches, and strategies are treated as first-order objects.  Thus the
second-order quantification over edge predicates is represented by predicates
over reified objects:
\begin{lstlisting}
NEW-SPANNING-TREE(G,T)
NEW-MINIMUM-TOTAL-WEIGHT(G,T)
NEW-CUT-RESPECTED(G,F,CUT)
NEW-SAFE-EDGE(G,F,E)
NEW-SAFE-BATCH(G,F,BATCH)
\end{lstlisting}
Reachability and weight comparison remain auxiliary predicates, just as in the
input specification.  This keeps the logical surface close to the mathematical
contract while making each refinement clause a first-order SNARK formula.

\subsection{The Abstract Greedy Program}

The first executable-looking answer is not any named MST algorithm.  It is the
shared greedy recurrence:
\[
  F\ \hbox{complete} \Rightarrow F,\qquad
  F\ \hbox{incomplete} \Rightarrow
  \mathrm{Greedy}(G,\mathrm{Extend}(G,F)).
\]
The extracted answer is therefore a program context with a single open
operation, \texttt{NEW-MST-EXTEND}.  That operation is where the later
refinements plug in:
\begin{lstlisting}
(ANSWER-IF (NEW-MST-COMPLETE G FOREST)
  (VALUES FOREST)
  (VALUES (NEW-MST-GREEDY G (NEW-MST-EXTEND G FOREST))))
\end{lstlisting}
This is the key abstraction boundary.  The proof term has committed to an
iterative greedy construction, but not to the policy used to find the next safe
extension.

\subsection{Safe Extensions and the Cut Property}

The next layer splits extension into two cases: add one safe edge, or add a
safe batch.  This is the point at which Kruskal and Prim/Jarnik can share one
single-edge interface while Boruvka can use a batch interface:
\begin{lstlisting}
(ANSWER-IF (NEW-SAFE-EDGE G FOREST EDGE)
  (ANSWER-IF (NEW-SAFE-BATCH G FOREST BATCH)
    (VALUES (NEW-MST-ADD-BATCH BATCH FOREST))
    (VALUES (NEW-MST-ADD-EDGE EDGE FOREST)))
  (VALUES (NEW-MST-ADD-BATCH BATCH FOREST)))
\end{lstlisting}
The cut-property refinement then justifies a safe single-edge extension:
\[
  \mathrm{CutRespected}(G,F,C)\land
  \mathrm{Crosses}(e,C)\land
  \mathrm{LightestCrossing}(G,e,C)
  \Rightarrow F+e.
\]
SNARK extracts:
\begin{lstlisting}
(VALUES (NEW-MST-ADD-EDGE EDGE FOREST))
\end{lstlisting}
This is the logical hinge: once an algorithm can be shown to choose an edge or
batch satisfying the relevant cut/safety predicate, it inherits the same
extension shape.

\subsection{Algorithm Families as Refinements}

The named algorithms enter only after the shared proof obligations are in
place:
\begin{itemize}
\item Kruskal refines extension by choosing a lightest edge that connects two
different forest components.
\item Jarnik--Prim refines extension by choosing a lightest edge on the
frontier of one growing component.
\item Boruvka refines extension by choosing a batch of lightest outgoing edges,
one per current component.
\end{itemize}
The extracted answers make their common structure explicit:
\begin{lstlisting}
(KRUSKAL-REFINEMENT
  (VALUES (NEW-MST-ADD-EDGE EDGE FOREST)))
(PRIM-REFINEMENT
  (VALUES (NEW-MST-ADD-EDGE EDGE FOREST)))
(BORUVKA-REFINEMENT
  (VALUES (NEW-MST-ADD-BATCH BATCH FOREST)))
\end{lstlisting}
Kruskal and Prim/Jarnik differ in the predicate used to produce \texttt{EDGE};
Boruvka differs in producing \texttt{BATCH}.  The answer terms expose exactly
that difference and no more.

\subsection{Concrete Implementations}

The executable layer interprets the symbolic leaves over finite Lisp graphs.
Edges are records \((id,u,v,w)\), forests are maintained with a union-find
structure, and the three strategies share the same output contract:
\begin{lstlisting}
(new-run-mst graph :strategy :kruskal)
(new-run-mst graph :strategy :prim)
(new-run-mst graph :strategy :boruvka)
\end{lstlisting}
On the demo graph, all three implementations return spanning forests of total
weight \(9\).  Their edge orders differ because the concrete algorithms expose
different traversal and tie-breaking choices, but they instantiate the same
abstract refinement chain.

\subsection{Optimization and Certification Refinements}

The advanced MST file adds a second ladder on top of the basic one.  It starts
from the blue/cut rule already used above and adds the red/cycle rule, so the
development can both add safe edges and remove provably useless edges.  That
gives the contract/delete normalization pattern:
\[
  \mathrm{Safe}(e)\Rightarrow \mathrm{contract}(e),\qquad
  \mathrm{Useless}(e)\Rightarrow \mathrm{delete}(e).
\]
Reverse-delete and Filter-Kruskal use deletion directly
\cite{CheritonTarjan1976,OsipovSandersSingler2009}.  Karger--Klein--Tarjan
and soft-heap filtering use the same rule after sampling or approximate
candidate generation \cite{KargerKleinTarjan1995,Chazelle2000}.

The same suite separates correctness from data-structure selection.  Kruskal
can be a sorted scan, a union-find scan, or a filtering algorithm.
Prim/Jarnik can use a dense scan or priority-queue ADTs, including Fibonacci
heaps \cite{FredmanTarjan1987}.  These refinements do not change the
mathematical witness relation; they refine the implementation of the symbolic
operations that the proof term names.

Finally, the suite adds certifying and sensitivity-oriented developments:
verification by the tree-path maximum condition, replacement edges for every
tree edge, a second-best MST witness, offline dynamic-update hooks, and a
GHS-style distributed phase
\cite{DixonRauchTarjan1992,Eppstein1994,GallagerHumbletSpira1983}.  These are
not merely more ways to compute the same tree.  They show that answer
extraction can target checkers, repair tables, and algorithm-family interfaces
as well as direct constructors.

\section{Limitations}

These examples are deliberately small.  They are working SNARK demonstrations
of answer extraction for classical synthesis patterns, not full reconstructions
of Manna and Waldinger's deductive-tableau derivations.  In particular:
\begin{itemize}
\item the square-root examples use abstract arithmetic predicates rather than a
complete theory of real numbers;
\item the GCD and quotient/remainder examples similarly use abstract integer
comparison and subtraction predicates in the proof layer;
\item the new stable-merge, stable-partition, and IF-expression examples use
abstract list and IF-expression theories;
\item the strict MST FOL layer replaces the non-probabilistic named
obligations with finite first-order certificates, but it still uses explicit
finite certificates rather than synthesizing induction principles over
arbitrary list length;
\item the broader MST prelude reifies paths, cuts, cycles, candidate trees,
weight comparisons, exchanges, contractions, samples, heap states, matroids,
update streams, and message schedules as first-order objects;
\item probabilistic and amortized-time claims, such as the expected-time KKT
bound or soft-heap complexity analysis, are not themselves proved in FOL here;
the FOL layer proves the corresponding correctness/refinement terms over
reified samples and heap states;
\item the unification examples isolate key branches rather than proving a
single global total-correctness theorem for all expression pairs;
\item induction and termination arguments are represented by recursive
skeleton symbols rather than synthesized from a full well-founded induction
proof.
\end{itemize}
These choices keep the examples runnable and inspectable inside the existing
SNARK example suite while preserving the core answer-extraction phenomenon.

\section{Running the Examples}

After rebuilding SNARK, start the local executable and enter the SNARK user
package:
\begin{lstlisting}
./snark
(in-package :snark-user)
\end{lstlisting}
Then run:
\begin{lstlisting}
(mw-synthesis-examples)
(new-synthesis-examples)
(new-mst-fol-synthesis-examples)
(new-mst-prelude-synthesis-examples)
(new-mst-synthesis-examples)
(new-mst-advanced-synthesis-examples)
\end{lstlisting}
The result is a list pairing each example name with its extracted answer term.

\section{From Proof Answers to Executable Code}

The SNARK proof answers are executable-looking, but they are still symbolic
terms in an object language.  For example, the unification proof answer contains
\texttt{MW-OCCURS}, \texttt{MW-UNIFY}, \texttt{MW-SUBST-OF}, and
\texttt{MW-COMPOSE}.  The square-root proof answer contains \texttt{MW-LE},
\texttt{MW-SQUARE}, \texttt{MW-PLUS}, and \texttt{MW-SQRT}.  The GCD and
quotient/remainder answers contain \texttt{MW-GT}, \texttt{MW-LT},
\texttt{MW-MINUS}, \texttt{MW-GCD}, \texttt{MW-QR}, and pair projection
symbols.  The new examples similarly contain abstract list constructors,
IF-expression accessors, path-assumption predicates, and recursive
simplification calls.  The strict MST layer starts from finite certificates:
\begin{lstlisting}
MSTF-MEMBER, MSTF-PATH-LIST, MSTF-CROSSES-CUT,
MSTF-DOMINATES-LIST
\end{lstlisting}
From those certificates, SNARK extracts:
\begin{lstlisting}
MSTF-EXCHANGE, MSTF-KRUSKAL-STEP,
MSTF-PRIM-STEP, MSTF-BORUVKA-PHASE
\end{lstlisting}
The broader MST prelude still contains abstract predicates for filtering, soft
heaps, matroids, replacement, and message schedules.  SNARK has proved that
witnesses with these shapes exist under the stated finite certificates; it has
not, by itself, supplied concrete Common Lisp definitions for those abstract
symbols.

The distribution therefore includes explicit executable interpretations.  For
square root, the recursive skeleton becomes:
\begin{lstlisting}
(defun mw-run-sqrt-step (r eps z)
  (if (<= (* (+ z eps) (+ z eps)) r)
      (+ z eps)
      z))

(defun mw-run-sqrt (r eps)
  (if (< (max r 1) eps)
      0
      (let ((z (mw-run-sqrt r (* 2 eps))))
        (mw-run-sqrt-step r eps z))))
\end{lstlisting}
For unification, \texttt{mw-run-unify} implements the generated branches using
concrete predicates for variables, constants, applications, occurs-check
failure, substitution application, and substitution composition.  A successful
sample call is:
\begin{lstlisting}
(mw-run-unify
 '(mw-app (mw-const f) (mw-var x))
 '(mw-app (mw-const f) (mw-const a)))
\end{lstlisting}
which returns a successful substitution binding \texttt{x} to
\texttt{(mw-const a)}.

For the arithmetic examples, the runners instantiate the symbolic comparisons
and recursive calls over Common Lisp integers:
\begin{lstlisting}
(defun mw-run-gcd (a b)
  (cond
   ((= a b) a)
   ((> a b) (mw-run-gcd (- a b) b))
   (t (mw-run-gcd a (- b a)))))

(defun mw-run-quotient-remainder (n d)
  (if (< n d)
      (list :quotient 0 :remainder n)
      (let ((qr (mw-run-quotient-remainder (- n d) d)))
        (list :quotient (1+ (getf qr :quotient))
              :remainder (getf qr :remainder)))))
\end{lstlisting}
The following command runs all executable demonstrations:
\begin{lstlisting}
(mw-executable-synthesis-demos)
(new-executable-synthesis-demos)
(new-run-mst-demo)
(new-run-mst-advanced-demo)
\end{lstlisting}
This split is deliberate: the proof answers document the synthesized control
structure; the \texttt{MW-RUN-*} and \texttt{NEW-RUN-*} layers supply the
host-language semantics needed to execute it.

\appendix

\section{Prompts and Reproducibility Log}

This appendix records the user-facing prompts and the operational command log
used to produce the development.  It does not include private chain-of-thought.
For reproducibility, the private deliberation is replaced by concise reasoning
summaries and concrete commands.

\subsection{User Prompts}

\begin{enumerate}
\item
\begin{lstlisting}
Compile SNARK for my Mac.
\end{lstlisting}
\item
\begin{lstlisting}
SNARK comes with the capability of answer extraction, which can be used for program synthesis. Manna/Waldinger did this for deductively synthesizing, among other things, the unification algorithm, binary search (via a specification for computing the square root), etc. Look these up and redo these examples in SNARK, under examples/synthesis.
\end{lstlisting}
\item
\begin{lstlisting}
Under docs/synthesis, make a TeX document outlining this new development. Include the classical examples as your two working examples. In it, outline the development, but also include the prompts and the chain-of-thought/agent commands you've applied in an appendix. Use LNCS format for this work.
\end{lstlisting}
\item
\begin{lstlisting}
Add a bunch of the generated files (fasls, TeX files) and stuff to a gitignore.
\end{lstlisting}
\item
\begin{lstlisting}
For the examples we have, how can we run the generated programs?
\end{lstlisting}
\item
\begin{lstlisting}
Can these be included in the distribution and the writeup, to demonstrate the gap between the SNARK proof term and the true executable code?
\end{lstlisting}
\item
\begin{lstlisting}
What are other interesting, non-trivial examples like unification and binary search that we can do? Emphasis on the non-trivial.
\end{lstlisting}
\item
\begin{lstlisting}
Yes, let's do GCD and quotient remainder (and put those in the existing mw-... suite, since those are examples they did -- cite those works in the paper too).
\end{lstlisting}
\item
\begin{lstlisting}
Now, add a new file with "new" synthesis examples (since the ones we've done are all prior Manna/Waldinger examples). What are non-trivial examples that can really exercise this paradigm?
\end{lstlisting}
\item
\begin{lstlisting}
In the documentation, for each example, could you include the (1) mathematical/logical specification (2) that specification in SNARK (the input to the theorem prover) and (3) the answer? Right now, we don't have the intermediate SNARK representation.
\end{lstlisting}
\item
\begin{lstlisting}
The addition simplification and shortest-path relaxation are kind of lame. Remove those and instead, do the following example (taken from Boyer/Moore, A Computational Logic):

IF-expressions can be defined by a datatype that has a constructor for IF-expressions with three accessors: condition, thenBranch, and elseBranch, and the truth values: True and False, and variables. An IF-expression is in normal form if the condition part of any IF-expression is always a variable, and there are no redundant IF-expressions of the form IF(x, A, A). Write a simplifier for normal-form IF-expressions so that no variable is repeated along any branch.
\end{lstlisting}
\item
\begin{lstlisting}
Under new examples, let's do a thorough MST development. I want to do an abstract --> concrete MST algorithm development that doesn't immediately fix Kruskal/Jarnik-Prim/Boruvka but structures them abstractly and as a chain of logical refinements.

Please encode the algorithm abstractly and hierarchically frame the various algorithms as logical refinements, with the SNARK conditions/specifications and the subsequent answer extraction providing the refinements and their direct implementations.
\end{lstlisting}
\item
\begin{lstlisting}
There's a whole literature on MST algorithm development. What other examples, refinements, and optimizations can we encode logically and generate algorithms/code via SNARK for?
\end{lstlisting}
\item
\begin{lstlisting}
Ok, yes -- let's do all of these developments. Please implement these, show their SNARK derivation/answer extractions, and catalogue all of these variants.
\end{lstlisting}
\item
\begin{lstlisting}
I want everything proven from first principles in FOL. The point is not to shortcut ANYTHIING.
\end{lstlisting}
\item
\begin{lstlisting}
Yes, reify these via an MST prelude development. Get the appropriate FOL objects/concepts, the corresponding lemmas (and their corresponding answer extraction/program terms), and then the algorithms.
\end{lstlisting}
\end{enumerate}

\subsection{Research Queries and Sources Consulted}

The literature lookup used the following public-search queries:
\begin{lstlisting}
Manna Waldinger deductive synthesis unification algorithm program synthesis
Manna Waldinger binary search square root deductive synthesis
Manna Waldinger deductive synthesis square root binary search specification
"Deductive synthesis of the unification algorithm" pdf
"The origin of a binary-search paradigm" "sqrt" pdf
"Automatic Deductive Synthesis of the Unification Algorithm" SNARK Waldinger pdf
Manna Waldinger quotient remainder deductive program synthesis
Manna Waldinger gcd intermittent assertions program correctness
"Manna" "Waldinger" "greatest common divisor" "gcd"
"Is 'Sometime' Sometimes Better Than 'Always'" "Manna" "Waldinger" gcd
Boyer Moore A Computational Logic IF expression normal form simplifier
"A Computational Logic" Boyer Moore IF expressions normal form
minimum spanning tree algorithm development literature Boruvka Kruskal Prim
Karger Klein Tarjan randomized linear time minimum spanning tree
Cheriton Tarjan finding minimum spanning trees
Fredman Tarjan Fibonacci heaps minimum spanning tree
Chazelle minimum spanning tree inverse Ackermann
Pettie Ramachandran optimal minimum spanning tree algorithm
Tarjan verification sensitivity analysis minimum spanning trees
Gallager Humblet Spira distributed minimum weight spanning trees
Filter-Kruskal minimum spanning tree algorithm
Edmonds matroids and the greedy algorithm
\end{lstlisting}
The most relevant sources were the SRI pages and PDFs for unification and
binary search, the IJCAI short paper on binary search, the SRI page for the
TOPLAS deductive-synthesis paper, the SRI page for the intermittent-assertion
paper, the Isabelle theory page summarizing a Manna--Waldinger-style
unification formalization, Boyer and Moore's \emph{A Computational Logic}, and
the MST literature cited in the bibliography.

\subsection{Concise Reasoning Summary}

\begin{enumerate}
\item Inspect the repository to identify build scripts and existing example
style.
\item Build SNARK with the installed SBCL, because the checked-in 64-bit SBCL
script points at an obsolete local path.
\item Study existing answer-extraction examples, especially the front/last and
list-reversal examples.
\item Recast the two classical synthesis developments as small SNARK
existence proofs whose answers are program fragments.
\item Wire the new examples into both the manual system loader and ASDF example
system.
\item Rebuild SNARK and verify that the saved native image runs the new
examples directly.
\item Write this LNCS-format document under \texttt{docs/synthesis}.
\item Add executable interpretations for the symbolic answer terms to make the
proof-answer/executable-code gap concrete.
\item Extend the Manna--Waldinger suite with GCD and quotient/remainder
examples and cite their classical sources.
\item Add a separate new-example suite for stable merge, stable partition, and
a Boyer--Moore IF-expression simplifier.
\item Extend the writeup with a catalogue that exposes each example's
mathematical specification, SNARK prover input, and extracted answer.
\item Replace the weaker addition and shortest-path examples with the
normal-form IF-expression simplifier and rebuild the distribution.
\item Add a separate MST refinement suite that starts from a reified
first-order MST postcondition, refines through safe extensions and the cut
property, and interprets Kruskal, Prim/Jarnik, and Boruvka as concrete
strategy refinements.
\item Add an advanced MST suite covering red-rule deletion, reverse-delete,
contract/delete normalization, full Boruvka phases, Kruskal and Prim
implementation policies, MST verification, replacement edges, KKT filtering,
soft-heap repair, matroid greedy abstraction, offline dynamic hooks, and
GHS-style distributed phases.
\item Add an MST prelude suite that reifies the FOL objects used by those
developments and proves the core schematic lemma terms before the algorithm
layer consumes them.
\end{enumerate}

\section{IF-Normalization Dialogue Example}

This appendix section records the IF-normalization exchange as a focused
example of how the synthesis suite evolved.  It includes the user prompt and
the public reasoning used to translate that prompt into the final SNARK
example.  It does not include private chain-of-thought.

\subsection{Prompt}

\begin{lstlisting}
The addition simplification and shortest-path relaxation are kind of lame. Remove those and instead, do the following example (taken from Boyer/Moore, A Computational Logic):

IF-expressions can be defined by a datatype that has a constructor for IF-expressions with three accessors: condition, thenBranch, and elseBranch, and the truth values: True and False, and variables. An IF-expression is in normal form if the condition part of any IF-expression is always a variable, and there are no redundant IF-expressions of the form IF(x, A, A). Write a simplifier for normal-form IF-expressions so that no variable is repeated along any branch.
\end{lstlisting}

\subsection{Public Reasoning}

The design correction was to remove the two weaker examples rather than keep
them as clutter.  The IF-normalization problem is a better stress test for
answer extraction because it combines recursive descent, branch-local
assumptions, equality of recursively simplified branches, and a normalization
invariant.

The SNARK object language used these symbols:
\begin{lstlisting}
NEW-IF, NEW-CONDITION, NEW-THEN-BRANCH, NEW-ELSE-BRANCH
NEW-ASSUMED-TRUE, NEW-ASSUMED-FALSE
NEW-EXTEND-TRUE, NEW-EXTEND-FALSE
NEW-SIMPLIFY-IF
NEW-SAME
\end{lstlisting}
The predicate \texttt{NEW-SAME} is the branch equality test used to collapse
\(\mathsf{IF}(x,A,A)\) after recursive simplification.

The first natural encoding treated the whole datatype at once: truth constants,
variables, and IF nodes.  That was too broad for this compact example harness,
so the SNARK proof obligation was narrowed to the recursive IF-node case while
the executable interpretation retained the complete leaf semantics.  This keeps
the extracted answer focused on the interesting synthesis step:
\begin{enumerate}
\item if the condition is already assumed true, simplify only the then-branch;
\item if the condition is already assumed false, simplify only the else-branch;
\item otherwise simplify both branches under extended assumptions;
\item if those simplified branches are equal, return one branch;
\item otherwise rebuild the IF node from the simplified branches.
\end{enumerate}

One additional consistency fact was added:
\begin{lstlisting}
(assert '(not (and (new-assumed-true (new-condition expr) env)
                   (new-assumed-false (new-condition expr) env)))
        :name 'new-if-assumptions-consistent)
\end{lstlisting}
This prevents the extracted answer from containing an impossible case where a
condition is simultaneously known true and false.

The executable demonstration then checks the host-language side of the gap.  A
normal-form expression with repeated tests of \(x\) along branches is simplified
to:
\begin{lstlisting}
(NEW-IF (NEW-VAR Y) NEW-TRUE NEW-FALSE)
\end{lstlisting}
which has no repeated variable along either branch and no redundant
\(\mathsf{IF}(x,A,A)\) subexpression.

\subsection{Representative Commands}

\begin{lstlisting}
rg -n "addition|simplifier|shortest|relax|graph|new-plus|new-shortest" \
  docs/synthesis/mw-snark-synthesis.tex \
  examples/synthesis/README \
  examples/synthesis/new-synthesis-examples.lisp

apply_patch
# Removed new-plus-simplifier-program and new-shortest-path-relaxation-program.
# Added new-if-normal-form-simplifier-program and new-run-if-simplify.

./snark --noinform --disable-debugger \
  --eval '(in-package :snark-user)' \
  --eval '(load "examples/synthesis/new-synthesis-examples.lisp")' \
  --eval '(format t "~S~%" (new-synthesis-examples))' \
  --eval '(format t "~S~%" (new-executable-synthesis-demos))' \
  --eval '(sb-ext:quit)'

pkill -f './snark --noinform --disable-debugger'

./make-snark-sbcl

./snark --noinform --disable-debugger \
  --eval '(in-package :snark-user)' \
  --eval '(format t "~S~%" (new-synthesis-examples))' \
  --eval '(format t "~S~%" (new-executable-synthesis-demos))' \
  --eval '(sb-ext:quit)'

latexmk -pdf -interaction=nonstopmode mw-snark-synthesis.tex
\end{lstlisting}

\section{MST Refinement Dialogue and Scripts}

This appendix section records the public interaction for the MST development.
As above, it omits private chain-of-thought and records only prompts, public
status/reasoning, scripts, and observed outputs.

\subsection{Prompt}

\begin{lstlisting}
Under new examples, let's do a thorough MST development. I want to do an abstract --> concrete MST algorithm development that doesn't immediately fix Kruskal/Jarnik-Prim/Boruvka but structures them abstractly and as a chain of logical refinements.

Please encode the algorithm abstractly and hierarchically frame the various algorithms as logical refinements, with the SNARK conditions/specifications and the subsequent answer extraction providing the refinements and their direct implementations.
\end{lstlisting}
The prompt then supplied a many-sorted relational MST specification with
\texttt{Inc}, \texttt{InG}, \texttt{T}, \texttt{w}, reachability predicates,
a reified competing selection \texttt{S}, \texttt{WeightLeq}, and an optional
cut-property formulation.  The implementation keeps that intention but reifies
trees, cuts, batches, forests, and strategies as first-order objects.

\subsection{Public Codex Interactions}

\begin{lstlisting}
This is a bigger architectural pass, so I'll treat MST as its own new synthesis file and then thread it into the loader, examples README, and LNCS writeup.

I'm going to keep the SNARK layer intentionally first-order by reifying trees, cuts, batches, forests, and strategies as objects, then use concrete Lisp runners for the executable side.

The MST file is written with eight SNARK refinement examples and concrete finite-graph runners. I found two issues in the first smoke run: the single-premise refinements needed their premises asserted for answer extraction, and one cut predicate arity was inconsistent.

The concrete runners agree on weight 9 across Kruskal, Prim, and Boruvka, which is exactly the executable sanity check I wanted before wiring it into the rebuilt image.

The refinement proofs now all extract witnesses. The dispatcher still carries some impossible strategy combinations in the nested ANSWER-IF term, so the writeup calls out the distinction between SNARK's proof answer and the concrete dispatch layer.

The rebuilt image now includes the MST suite directly, and I'm adding the larger theory section plus the reproducibility appendix entries to the LNCS document.
\end{lstlisting}

\subsection{Scripts and Outputs}

\begin{lstlisting}
sed -n '1,260p' examples/synthesis/new-synthesis-examples.lisp
sed -n '1,220p' snark-system.lisp
sed -n '1,120p' snark-examples.asd

apply_patch
# Added examples/synthesis/new-mst-synthesis.lisp.
# Updated snark-system.lisp, snark-examples.asd, and examples/synthesis/README.

./snark --noinform --disable-debugger \
  --eval '(in-package :snark-user)' \
  --eval '(load "examples/synthesis/new-mst-fol-synthesis.lisp")' \
  --eval '(format t "~S~%" (new-mst-fol-synthesis-examples))' \
  --eval '(sb-ext:quit)'

./snark --noinform --disable-debugger \
  --eval '(in-package :snark-user)' \
  --eval '(load "examples/synthesis/new-mst-synthesis.lisp")' \
  --eval '(format t "~S~%" (new-mst-synthesis-examples))' \
  --eval '(format t "~S~%" (new-run-mst-demo))' \
  --eval '(sb-ext:quit)'
\end{lstlisting}
The initial script exposed two public corrections: \texttt{NEW-CROSSES-CUT}
was declared with the wrong arity for the cut-property formula, and the
single-branch refinements needed their premises asserted so that SNARK could
derive a witness rather than returning no answer.

\begin{lstlisting}
./make-snark-sbcl

./snark --noinform --disable-debugger \
  --eval '(in-package :snark-user)' \
  --eval '(format t "~S~%" (new-mst-synthesis-examples))' \
  --eval '(format t "~S~%" (new-run-mst-demo))' \
  --eval '(sb-ext:quit)'
\end{lstlisting}
The rebuilt image produced the following headline MST outputs:
\begin{lstlisting}
((ABSTRACT-SPECIFICATION (VALUES TREE))
 (ABSTRACT-GREEDY-REFINEMENT
  (ANSWER-IF (NEW-MST-COMPLETE G FOREST)
    (VALUES FOREST)
    (VALUES (NEW-MST-GREEDY G (NEW-MST-EXTEND G FOREST)))))
 (SAFE-EXTENSION-REFINEMENT
  (ANSWER-IF (NEW-SAFE-EDGE G FOREST EDGE)
    (ANSWER-IF (NEW-SAFE-BATCH G FOREST BATCH)
      (VALUES (NEW-MST-ADD-BATCH BATCH FOREST))
      (VALUES (NEW-MST-ADD-EDGE EDGE FOREST)))
    (VALUES (NEW-MST-ADD-BATCH BATCH FOREST))))
 (CUT-PROPERTY-REFINEMENT
  (VALUES (NEW-MST-ADD-EDGE EDGE FOREST)))
 (KRUSKAL-REFINEMENT
  (VALUES (NEW-MST-ADD-EDGE EDGE FOREST)))
 (PRIM-REFINEMENT
  (VALUES (NEW-MST-ADD-EDGE EDGE FOREST)))
 (BORUVKA-REFINEMENT
  (VALUES (NEW-MST-ADD-BATCH BATCH FOREST))))

(:KRUSKAL (:EDGES (BC DE BD EF AC) :WEIGHT 9)
 :PRIM (:EDGES (AC BC BD DE EF) :WEIGHT 9)
 :BORUVKA (:EDGES (EF DE BC AC BD) :WEIGHT 9)
 :WEIGHTS-AGREE T)
\end{lstlisting}
Finally, the document build was rerun:
\begin{lstlisting}
latexmk -pdf -interaction=nonstopmode mw-snark-synthesis.tex
rg -n "undefined|Undefined|Citation .* undefined|LaTeX Error|Emergency stop|Fatal error|Overfull" \
  docs/synthesis/mw-snark-synthesis.log
\end{lstlisting}

\section{Strict MST FOL Layer Dialogue and Scripts}

This appendix records the public interaction for the strict MST FOL layer.

\subsection{Prompt}

\begin{lstlisting}
Let's finish the fianl layer of the non-probabilistic MST pipeline. Replace the named MST obligations with FOL developments for finite sets/lists, paths, cuts, components, acyclicity, finite weight comparison, exchange, blue rule, red rule, batch safety, and then derive Kruskal/Prim/Boruvka as consequences of those lemmas.
\end{lstlisting}

\subsection{Public Codex Interactions}

\begin{lstlisting}
I'll turn this into another concrete implementation pass rather than just tightening prose: add the lower FOL layer, thread the derived obligations through the MST prelude/algorithms, update the catalogue, and rerun the SNARK/PDF checks.

The existing prelude has the right top-level lemmas, but the premise layer is still too chunky. I'm going to add a new bottom layer that uses finite-list/set/path/cut/weight certificate relations and then have the non-probabilistic algorithm derivations consume those lower-level derived facts rather than the old named obligations.

The strict layer is in place now as a new file. I'm going to run it directly before wiring it into the image, because fresh SNARK declarations tend to catch arity/name mistakes immediately.
\end{lstlisting}

\subsection{Scripts and Outputs}

\begin{lstlisting}
apply_patch
# Added examples/synthesis/new-mst-fol-synthesis.lisp.
# Updated snark-system.lisp, snark-examples.asd, README, and this TeX file.

./snark --noinform --disable-debugger \
  --eval '(in-package :snark-user)' \
  --eval '(load "examples/synthesis/new-mst-fol-synthesis.lisp")' \
  --eval '(format t "~S~%" (new-mst-fol-synthesis-examples))' \
  --eval '(sb-ext:quit)'
\end{lstlisting}
The extracted answers include:
\begin{lstlisting}
(EXCHANGE-FROM-PATH-CUT-WEIGHT
  (VALUES (MSTF-EXCHANGE TREE OLD EDGE)))

(BLUE-RULE-FROM-EXCHANGE
  (VALUES
    (MSTF-EXCHANGE TREE (MSTF-CROSSING-EDGE PATH CUT) EDGE)))

(KRUSKAL-FROM-BLUE-RULE
  (VALUES (MSTF-KRUSKAL-STEP G (MSTF-ADD-EDGE EDGE FOREST))))

(PRIM-FROM-BLUE-RULE
  (VALUES (MSTF-PRIM-STEP G (MSTF-ADD-EDGE EDGE FOREST) ROOT)))

(BORUVKA-FROM-BATCH-SAFETY
  (VALUES (MSTF-BORUVKA-PHASE G (MSTF-CONTRACT-SET G BATCH))))
\end{lstlisting}

\section{MST Prelude Dialogue and Scripts}

This appendix section records the public interaction for the no-shortcut MST
prelude pass.

\subsection{Prompt}

\begin{lstlisting}
Yes, reify these via an MST prelude development. Get the appropriate FOL objects/concepts, the corresponding lemmas (and their corresponding answer extraction/program terms), and then the algorithms.
\end{lstlisting}

\subsection{Public Codex Interactions}

\begin{lstlisting}
That changes the standard from "refinement catalogue" to "formal development." Pure FOL can do this only if we reify paths, finite sets, sums/orders, samples, heaps, and schedules as first-order objects.

The current files still use the shortcuts you're objecting to: predicates like NEW-HEAVIEST-ON-CYCLE, NEW-SAFE-EDGE, NEW-F-HEAVY-FILTERED, NEW-CORRUPTION-REPAIR-VALID, and NEW-GRAPHIC-MATROID-OF are being asserted as premises.

I'm going to add this as a new new-mst-prelude-synthesis.lisp rather than burying it in the existing advanced file. That gives us a clean line: old advanced file is the scaffold; the prelude file is where the first-principles FOL objects and derived lemmas live.

The first prelude smoke run hit a search blow-up around the quantified blue-rule imports. I stopped the stuck image and tightened the prelude into the same schematic style SNARK's existing synthesis examples use.
\end{lstlisting}

\subsection{Scripts and Outputs}

\begin{lstlisting}
rg -n "new-(safe|useless|heaviest|lightest|spanning|minimum|components|each|path|max|replacement|sample|f-heavy|soft|corruption|matroid|graphic|update|ghs|fragments|tree-spans|tree-acyclic)" \
  examples/synthesis/new-mst-synthesis.lisp \
  examples/synthesis/new-mst-advanced-synthesis.lisp

apply_patch
# Added examples/synthesis/new-mst-prelude-synthesis.lisp.
# Updated snark-system.lisp, snark-examples.asd, README, and this TeX file.

./snark --noinform --disable-debugger \
  --eval '(in-package :snark-user)' \
  --eval '(load "examples/synthesis/new-mst-prelude-synthesis.lisp")' \
  --eval '(format t "~S~%" (new-mst-prelude-synthesis-examples))' \
  --eval '(sb-ext:quit)'
\end{lstlisting}
The resulting prelude answers include:
\begin{lstlisting}
(BLUE-RULE-LEMMA
  (VALUES
    (MSTP-EXCHANGE TREE (MSTP-CROSSING-EDGE PATH CUT) EDGE)))

(RED-RULE-LEMMA
  (VALUES (MSTP-DELETE-EDGE EDGE G)))

(KKT-FILTER-LEMMA
  (VALUES (MSTP-KKT FILTERED RANDOM-SOURCE)))

(KRUSKAL-ALGORITHM
  (VALUES (MSTP-KRUSKAL-STEP G (MSTP-ADD-EDGE EDGE FOREST))))
\end{lstlisting}

\section{Advanced MST Dialogue and Scripts}

This appendix section records the public interaction for the advanced MST
development.

\subsection{Prompt}

\begin{lstlisting}
Ok, yes -- let's do all of these developments. Please implement these, show their SNARK derivation/answer extractions, and catalogue all of these variants.
\end{lstlisting}

\subsection{Public Codex Interactions}

\begin{lstlisting}
I'll treat this as a full extension pass: add a new advanced MST synthesis file, wire it into the image/loaders, add runnable finite-graph interpretations where sensible, and expand the LNCS catalogue/writeup with each SNARK input and extracted answer.

The new file will sit beside the base MST development and reuse its finite graph representation. It adds blue/red rules, contraction/deletion, algorithm-family refinements, verification, replacement edges, KKT/filtering, soft-heap abstraction, matroid greedy, and dynamic/distributed hooks.

The advanced proof suite and finite demo now both run from a loaded file. Next I'm wiring it into the normal SNARK image and README so it behaves like the existing synthesis examples instead of living off to the side.
\end{lstlisting}

\subsection{Scripts and Outputs}

\begin{lstlisting}
sed -n '1,520p' examples/synthesis/new-mst-synthesis.lisp
sed -n '1,170p' examples/synthesis/README

apply_patch
# Added examples/synthesis/new-mst-advanced-synthesis.lisp.
# Updated snark-system.lisp, snark-examples.asd, README, and this TeX file.

./snark --noinform --disable-debugger \
  --eval '(in-package :snark-user)' \
  --eval '(load "examples/synthesis/new-mst-advanced-synthesis.lisp")' \
  --eval '(format t "~S~%" (new-mst-advanced-synthesis-examples))' \
  --eval '(format t "~S~%" (new-run-mst-advanced-demo))' \
  --eval '(sb-ext:quit)'
\end{lstlisting}
The first smoke test exposed one public implementation correction: a local Lisp
helper was named \texttt{SEARCH}, shadowing the locked Common Lisp symbol.
Renaming the helper to \texttt{WALK} fixed the executable demonstration.

\begin{lstlisting}
./make-snark-sbcl

./snark --noinform --disable-debugger \
  --eval '(in-package :snark-user)' \
  --eval '(format t "~S~%" (new-mst-advanced-synthesis-examples))' \
  --eval '(format t "~S~%" (new-run-mst-advanced-demo))' \
  --eval '(sb-ext:quit)'

latexmk -pdf -interaction=nonstopmode mw-snark-synthesis.tex
\end{lstlisting}

\section{Representative Agent Commands}

\begin{lstlisting}
ls
git status --short
rg --files
find . -maxdepth 2 -iname '*readme*' -o -iname '*install*' -o -iname '*build*'

sed -n '1,220p' INSTALL
sed -n '1,220p' README
sed -n '1,240p' compile
sed -n '1,220p' make-snark-sbcl64
sed -n '1,220p' run-snark

command -v sbcl
command -v ccl
command -v ros
uname -m
sbcl --version
./make-snark-sbcl

ls -lh snark compile.out
file snark
tail -n 80 compile.out
rg -n "ERROR|Error|error|WARNING|Warning|warning|failed|Unhandled|debugger invoked" compile.out

./snark --noinform --disable-debugger \
  --eval '(in-package :snark-user)' \
  --eval '(reverse-example :length 2)' \
  --eval '(sb-ext:quit)'

sed -n '1,260p' examples/front-last-example.lisp
sed -n '1,260p' examples/coder-examples.lisp
rg -n "Answer|:answer|answer|values|declare-function|to-lisp|program" docs README examples src
sed -n '500,590p' src/main.lisp

apply_patch
# Added examples/synthesis/README
# Added examples/synthesis/synthesis-examples.lisp
# Updated snark-examples.asd
# Updated snark-system.lisp

./snark --noinform --disable-debugger \
  --eval '(in-package :snark-user)' \
  --eval '(load "examples/synthesis/synthesis-examples.lisp")' \
  --eval '(format t "~S~%" (mw-synthesis-examples))' \
  --eval '(sb-ext:quit)'

./make-snark-sbcl

./snark --noinform --disable-debugger \
  --eval '(in-package :snark-user)' \
  --eval '(format t "~S~%" (mw-synthesis-examples))' \
  --eval '(sb-ext:quit)'

./snark --noinform --disable-debugger \
  --eval '(in-package :snark-user)' \
  --eval '(format t "~S~%" (mw-executable-synthesis-demos))' \
  --eval '(sb-ext:quit)'

./snark --noinform --disable-debugger \
  --eval '(in-package :snark-user)' \
  --eval '(load "examples/synthesis/synthesis-examples.lisp")' \
  --eval '(format t "~S~%" (mw-synthesis-examples))' \
  --eval '(format t "~S~%" (mw-executable-synthesis-demos))' \
  --eval '(sb-ext:quit)'

./snark --noinform --disable-debugger \
  --eval '(in-package :snark-user)' \
  --eval '(load "examples/synthesis/new-synthesis-examples.lisp")' \
  --eval '(format t "~S~%" (new-synthesis-examples))' \
  --eval '(format t "~S~%" (new-executable-synthesis-demos))' \
  --eval '(sb-ext:quit)'

./snark --noinform --disable-debugger \
  --eval '(in-package :snark-user)' \
  --eval '(format t "~S~%" (new-synthesis-examples))' \
  --eval '(format t "~S~%" (new-executable-synthesis-demos))' \
  --eval '(sb-ext:quit)'

./snark --noinform --disable-debugger \
  --eval '(in-package :snark-user)' \
  --eval '(load "examples/synthesis/new-mst-synthesis.lisp")' \
  --eval '(format t "~S~%" (new-mst-synthesis-examples))' \
  --eval '(format t "~S~%" (new-run-mst-demo))' \
  --eval '(sb-ext:quit)'

./make-snark-sbcl

./snark --noinform --disable-debugger \
  --eval '(in-package :snark-user)' \
  --eval '(format t "~S~%" (new-mst-synthesis-examples))' \
  --eval '(format t "~S~%" (new-run-mst-demo))' \
  --eval '(sb-ext:quit)'

./snark --noinform --disable-debugger \
  --eval '(in-package :snark-user)' \
  --eval '(load "examples/synthesis/new-mst-advanced-synthesis.lisp")' \
  --eval '(format t "~S~%" (new-mst-advanced-synthesis-examples))' \
  --eval '(format t "~S~%" (new-run-mst-advanced-demo))' \
  --eval '(sb-ext:quit)'

./make-snark-sbcl

./snark --noinform --disable-debugger \
  --eval '(in-package :snark-user)' \
  --eval '(format t "~S~%" (new-mst-advanced-synthesis-examples))' \
  --eval '(format t "~S~%" (new-run-mst-advanced-demo))' \
  --eval '(sb-ext:quit)'

kpsewhich llncs.cls
command -v latexmk
command -v pdflatex
mkdir -p docs/synthesis
latexmk -pdf -interaction=nonstopmode mw-snark-synthesis.tex
\end{lstlisting}

\section{Extracted Answers}

The rebuilt executable produced the following answers for the headline working
examples.

\subsection{Unification}

\begin{lstlisting}
(UNIFICATION-VARIABLE-CASE
  (ANSWER-IF (MW-OCCURS (MW-VAR V) TERM)
    (VALUES MW-NONE)
    (VALUES (MW-SOME (MW-BIND V TERM)))))

(UNIFICATION-COMPOUND-CASE
  (ANSWER-IF (MW-FAIL (MW-UNIFY M M1))
    (VALUES MW-NONE)
    (ANSWER-IF
      (MW-FAIL
        (MW-UNIFY
          (MW-SUBST (MW-SUBST-OF (MW-UNIFY M M1)) N)
          (MW-SUBST (MW-SUBST-OF (MW-UNIFY M M1)) N1)))
      (VALUES MW-NONE)
      (VALUES
        (MW-SOME
          (MW-COMPOSE
            (MW-SUBST-OF (MW-UNIFY M M1))
            (MW-SUBST-OF
              (MW-UNIFY
                (MW-SUBST (MW-SUBST-OF (MW-UNIFY M M1)) N)
                (MW-SUBST (MW-SUBST-OF (MW-UNIFY M M1)) N1)))))))))
\end{lstlisting}

\subsection{Binary Search for Square Root}

\begin{lstlisting}
(SQUARE-ROOT-BINARY-SEARCH-STEP
  (ANSWER-IF (MW-LE (MW-SQUARE (MW-PLUS Z EPS)) R)
    (VALUES (MW-PLUS Z EPS))
    (VALUES Z)))

(SQUARE-ROOT-BINARY-SEARCH-PROGRAM
  (ANSWER-IF (MW-LARGE-TOLERANCE R EPS)
    (VALUES ZERO)
    (ANSWER-IF
      (MW-LE
        (MW-SQUARE
          (MW-PLUS (MW-SQRT R (MW-DOUBLE EPS)) EPS))
        R)
      (VALUES (MW-PLUS (MW-SQRT R (MW-DOUBLE EPS)) EPS))
      (VALUES (MW-SQRT R (MW-DOUBLE EPS))))))
\end{lstlisting}

\subsection{Greatest Common Divisor}

\begin{lstlisting}
(GCD-SUBTRACTIVE-PROGRAM
  (ANSWER-IF (MW-GT B A)
    (VALUES (MW-GCD A (MW-MINUS B A)))
    (ANSWER-IF (MW-GT A B)
      (VALUES (MW-GCD (MW-MINUS A B) B))
      (VALUES A))))
\end{lstlisting}

\subsection{Quotient and Remainder}

\begin{lstlisting}
(QUOTIENT-REMAINDER-PROGRAM
  (ANSWER-IF (MW-LT N D)
    (VALUES (MW-PAIR ZERO N))
    (VALUES
      (MW-PAIR
        (MW-SUCC (MW-QUOTIENT-OF (MW-QR (MW-MINUS N D) D)))
        (MW-REMAINDER-OF (MW-QR (MW-MINUS N D) D)))))))
\end{lstlisting}

\subsection{Strict MST FOL Layer}

\begin{lstlisting}
(FINITE-LIST-MEMBERSHIP
  (VALUES EDGE))

(INPUT-EDGE-FROM-FINITE-LIST
  (VALUES EDGE))

(PATH-FROM-FINITE-LIST
  (VALUES PATH))

(LIGHTEST-CROSSING-FROM-FINITE-COMPARISONS
  (VALUES EDGE))

(SPANNING-TREE-FROM-CERTIFICATES
  (VALUES TREE))

(EXCHANGE-FROM-PATH-CUT-WEIGHT
  (VALUES (MSTF-EXCHANGE TREE OLD EDGE)))

(BLUE-RULE-FROM-EXCHANGE
  (VALUES
    (MSTF-EXCHANGE TREE (MSTF-CROSSING-EDGE PATH CUT) EDGE)))

(RED-RULE-FROM-CYCLE
  (VALUES (MSTF-DELETE-EDGE EDGE G)))

(KRUSKAL-FROM-BLUE-RULE
  (VALUES (MSTF-KRUSKAL-STEP G (MSTF-ADD-EDGE EDGE FOREST))))

(PRIM-FROM-BLUE-RULE
  (VALUES (MSTF-PRIM-STEP G (MSTF-ADD-EDGE EDGE FOREST) ROOT)))

(BORUVKA-FROM-BATCH-SAFETY
  (VALUES (MSTF-BORUVKA-PHASE G (MSTF-CONTRACT-SET G BATCH))))
\end{lstlisting}

\section{Executable Demo Output}

Loading the synthesis example files and running the executable demo functions
produced:
\begin{lstlisting}
((UNIFICATION
  ((SUCCESS (MW-SOME ((X (MW-CONST A)))))
   (CONSTANT-CLASH MW-NONE)
   (OCCURS-CHECK MW-NONE)))
 (SQUARE-ROOT
  (:R 10 :EPS 1/100 :ANSWER 79/25 :WITHIN-SPEC T))
 (GCD
  (:A 84 :B 30 :ANSWER 6 :CL-GCD 6))
 (QUOTIENT-REMAINDER
  (:N 37 :D 5 :ANSWER (:QUOTIENT 7 :REMAINDER 2)
   :WITHIN-SPEC T)))

((STABLE-MERGE
  (:LEFT (1 3 5 7) :RIGHT (2 3 4 9)
   :ANSWER (1 2 3 3 4 5 7 9)))
 (STABLE-PARTITION
  (:INPUT (1 2 3 4 5 6 7)
   :ANSWER (:KEPT (1 3 5 7) :DROPPED (2 4 6))))
 (IF-NORMAL-FORM-SIMPLIFIER
  (:ANSWER (NEW-IF (NEW-VAR Y) NEW-TRUE NEW-FALSE))))

(:KRUSKAL (:EDGES (BC DE BD EF AC) :WEIGHT 9)
 :PRIM (:EDGES (AC BC BD DE EF) :WEIGHT 9)
 :BORUVKA (:EDGES (EF DE BC AC BD) :WEIGHT 9)
 :WEIGHTS-AGREE T)

(:REVERSE-DELETE (:EDGES (AC BC BD DE EF) :WEIGHT 9)
 :BORUVKA-PHASES
  (:PHASES ((EF DE BC AC) (BD)) :EDGES (EF DE BC AC BD)
   :WEIGHT 9)
 :VERIFICATION (:TREE-P T :PATH-MAX-CONDITION T :MST-P T)
 :SECOND-BEST
  (:TREE-EDGE AC :REPLACEMENT AB :DELTA 1
   :SECOND-BEST-WEIGHT 10)
 :KRUSKAL-VARIANTS
  ((:POLICY :SORTED-SCAN :WEIGHT 9)
   (:POLICY :UNION-FIND :WEIGHT 9)
   (:POLICY :FILTER-KRUSKAL :FILTER (DF CE) :WEIGHT 9))
 :PRIM-VARIANTS
  ((:POLICY :DENSE :QUEUE :ARRAY-SCAN :WEIGHT 9)
   (:POLICY :BINARY-HEAP :QUEUE :BINARY-HEAP :WEIGHT 9)
   (:POLICY :FIBONACCI-HEAP :QUEUE :FIBONACCI-HEAP :WEIGHT 9)
   (:POLICY :PAIRING-HEAP :QUEUE :PAIRING-HEAP :WEIGHT 9))
 :KKT-FILTER
  (:SAMPLE (AB BC CD DE EF) :FILTERED-OUT (DF CE)
   :EDGES (BC DE BD EF AC) :WEIGHT 9)
 :SOFT-HEAP
  (:CANDIDATE-FILTER (DF CE) :REPAIR :VERIFY-AFTER-CORRUPTION
   :WEIGHT 9)
 :MATROID-GREEDY (:MATROID :GRAPHIC :WEIGHT 9)
 :DYNAMIC
  ((:UPDATE (AB 1) :WEIGHT 7)
   (:UPDATE (DF 2) :WEIGHT 7))
 :GHS
  (:MESSAGE-RULE :MINIMUM-OUTGOING-EDGE
   :FRAGMENT-PHASES ((EF DE BC AC) (BD))
   :WEIGHT 9))
\end{lstlisting}
This output is intentionally different in kind from the extracted answers above:
it is the result of executing concrete Lisp functions that interpret the
symbols appearing in the proof terms.

\begin{thebibliography}{10}

\bibitem{MannaWaldinger1981}
Manna, Z., Waldinger, R.:
Deductive synthesis of the unification algorithm.
Science of Computer Programming 1(1--2), 5--48 (1981).
\url{https://www.sri.com/publication/artificial-intelligence-pubs/deductive-synthesis-of-the-unification-algorithm/}

\bibitem{Waldinger2021}
Waldinger, R.:
What, Again? Automatic Deductive Synthesis of the Unification Algorithm.
UNIF 2021 (2021).

\bibitem{IsabelleUnification}
Coen, M., Slind, K., Krauss, A.:
Isabelle/HOL theory \texttt{HOL-ex/Unification}.
\url{https://isabelle.in.tum.de/library/HOL/HOL-ex/Unification.html}

\bibitem{BoyerMoore1979}
Boyer, R.S., Moore, J.S.:
A Computational Logic.
ACM Monograph Series. Academic Press, New York (1979).
\url{https://www.cs.utexas.edu/~boyer/acl.pdf}

\bibitem{MannaWaldinger1978}
Manna, Z., Waldinger, R.:
Is ``sometime'' sometimes better than ``always''? Intermittent assertions in
proving program correctness.
Communications of the ACM 21(2), 159--172 (1978).
\url{https://doi.org/10.1145/359340.359353}

\bibitem{MannaWaldinger1985IJCAI}
Manna, Z., Waldinger, R.:
The Origin of the Binary-Search Paradigm.
Proceedings of IJCAI 1985, 223--225 (1985).
\url{https://www.ijcai.org/Proceedings/85-1/Papers/041.pdf}

\bibitem{MannaWaldinger1987}
Manna, Z., Waldinger, R.:
The origin of a binary-search paradigm.
Science of Computer Programming 9(1), 37--83 (1987).
\url{https://www.sri.com/publication/artificial-intelligence-pubs/the-origin-of-the-binary-search-paradigm/}

\bibitem{MannaWaldinger1980}
Manna, Z., Waldinger, R.:
A deductive approach to program synthesis.
ACM Transactions on Programming Languages and Systems 2(1), 90--121 (1980).
\url{https://doi.org/10.1145/357084.357090}

\bibitem{Kruskal1956}
Kruskal, J.B.:
On the shortest spanning subtree of a graph and the traveling salesman problem.
Proceedings of the American Mathematical Society 7(1), 48--50 (1956).
\url{https://cir.nii.ac.jp/crid/1363107368257159424}

\bibitem{Jarnik1930}
Jarnik, V.:
O jistem problemu minimalnim. (Z dopisu panu O. Boruvkovi).
Prace moravske prirodovedecke spolecnosti 6(4), 57--63 (1930).
\url{https://www.dml.cz/handle/10338.dmlcz/500726}

\bibitem{Prim1957}
Prim, R.C.:
Shortest connection networks and some generalizations.
Bell System Technical Journal 36(6), 1389--1401 (1957).
\url{https://www.nokia.com/bell-labs/publications-and-media/publications/shortest-connection-networks-and-some-generalizations/}

\bibitem{Boruvka1926}
Boruvka, O.:
O jistem problemu minimalnim.
Prace moravske prirodovedecke spolecnosti 3, 37--58 (1926).
\url{https://xlinux.nist.gov/dads/HTML/boruvkasAlgorithm.html}

\bibitem{CheritonTarjan1976}
Cheriton, D., Tarjan, R.E.:
Finding minimum spanning trees.
SIAM Journal on Computing 5(4), 724--742 (1976).
\url{https://epubs.siam.org/doi/10.1137/0205051}

\bibitem{FredmanTarjan1987}
Fredman, M.L., Tarjan, R.E.:
Fibonacci heaps and their uses in improved network optimization algorithms.
Journal of the ACM 34(3), 596--615 (1987).
\url{https://doi.org/10.1145/28869.28874}

\bibitem{DixonRauchTarjan1992}
Dixon, B., Rauch, M., Tarjan, R.E.:
Verification and sensitivity analysis of minimum spanning trees in linear time.
SIAM Journal on Computing 21(6), 1184--1192 (1992).
\url{https://doi.org/10.1137/0221070}

\bibitem{KargerKleinTarjan1995}
Karger, D.R., Klein, P.N., Tarjan, R.E.:
A randomized linear-time algorithm to find minimum spanning trees.
Journal of the ACM 42(2), 321--328 (1995).
\url{https://doi.org/10.1145/201019.201022}

\bibitem{Chazelle2000}
Chazelle, B.:
A minimum spanning tree algorithm with inverse-Ackermann type complexity.
Journal of the ACM 47(6), 1028--1047 (2000).
\url{https://doi.org/10.1145/355541.355562}

\bibitem{PettieRamachandran2002}
Pettie, S., Ramachandran, V.:
An optimal minimum spanning tree algorithm.
Journal of the ACM 49(1), 16--34 (2002).
\url{https://doi.org/10.1145/505241.505243}

\bibitem{Eppstein1994}
Eppstein, D.:
Offline algorithms for dynamic minimum spanning tree problems.
Journal of Algorithms 17(2), 237--250 (1994).
\url{https://doi.org/10.1006/jagm.1994.1033}

\bibitem{GallagerHumbletSpira1983}
Gallager, R.G., Humblet, P.A., Spira, P.M.:
A distributed algorithm for minimum-weight spanning trees.
ACM Transactions on Programming Languages and Systems 5(1), 66--77 (1983).
\url{https://dblp.uni-trier.de/rec/journals/toplas/GallagerHS83.html}

\bibitem{OsipovSandersSingler2009}
Osipov, V., Sanders, P., Singler, J.:
The Filter-Kruskal minimum spanning tree algorithm.
Proceedings of the 11th Workshop on Algorithm Engineering and Experiments,
52--61 (2009).
\url{https://publikationen.bibliothek.kit.edu/1000025373}

\bibitem{Edmonds1971}
Edmonds, J.:
Matroids and the greedy algorithm.
Mathematical Programming 1, 127--136 (1971).
\url{https://doi.org/10.1007/BF01584082}

\bibitem{SnarkGuide}
Stickel, M., Waldinger, R., Chaudhri, V.:
A Guide to SNARK.
\url{https://nilqed.github.io/SNARK/tutorial/tutorial.html}

\end{thebibliography}

\end{document}
